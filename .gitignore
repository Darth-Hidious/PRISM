# Claude Code session-local state — not for the repo
.claude/
.codex/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so
# Distribution / packaging
.Python
build/
develop-eggs/
dist/
.build/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
# PyInstaller
*.manifest
*.spec
# Installer logs
pip-log.txt
pip-delete-this-directory.txt
# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
# Translations
*.mo
*.pot
# Sphinx documentation
docs/_build/
# Generated docs (plans, designs) — but allow committed harness docs
docs/
!docs/tui-mcp-verification.md
!docs/examples/
!docs/examples/**
!docs/templates/
!docs/templates/**
# PyBuilder
target/
# Jupyter Notebook
.ipynb_checkpoints
# IPython
profile_default/
ipython_config.py
# pyenv
.python-version
# pipenv
Pipfile.lock
# PEP 582
__pypackages__/
# SageMath parsed files
*.sage.py
# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
# Spyder project settings
.spyderproject
.spyproject
# Rope project settings
.ropeproject
# mkdocs documentation
/site
# mypy
.mypy_cache/
.dmypy.json
dmypy.json
# Pyre type checker
.pyre/
# IDE
.vscode/
.idea/
*.swp
*.swo
*~
# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
# Application specific
*.log
*.db
*.sqlite3
provider_fields.json
models/*.joblib
models/*.pkl
models/*.meta.json
data/*.parquet
data/*.csv
data/*.meta.json
# Runtime data directories (Viking, vector DBs, etc.)
data/_system/
data/vectordb/
data/viking/
.openviking.pid
# Test artefacts (not shipped). The bare `tests/` line that used to live
# here ignored the entire test suite — wrong: test files belong in VCS so
# the suite is shareable + CI can run it. Setuptools controls packaging
# via the explicit `packages = [...]` list in pyproject.toml, not via
# .gitignore. Scoping to just the runtime artefacts the suite produces:
tests/_generated/
tests/.snapshot/
tests/coverage/
tests/htmlcov/
tests/.cache/
# Local clones / vendored deps
optimade-python-tools/
# Output artifacts
output/
node_modules/
extensions/*/out/
extensions/*/.vscode-test/
extensions/**/*.vsix
!extensions/*/docs/
!extensions/*/docs/**
.codebase-memory/
.superpowers/
.venv-mlx-gemma4/
.prism/
/tmp/prism-smoke/
# PRISM local-dev debris (do NOT commit)
# Engine debug tooling — committed at project scope on purpose
# .mcp.json  (committed)

# forge harness needs shell-plugin/lib at compile time (include_dir!)
# the generic 'lib/' ignore above (Python convention) was matching it
!shell-plugin/lib/
!shell-plugin/lib/**
/.mcp-tools/
