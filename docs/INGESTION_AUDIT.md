# Ingestion E2E Audit — 2026-07-06

> 71-agent workflow: 6 mappers x adversarial verifiers. 63 CONFIRMED / 2 refuted.
> Owner mandate: ingestion ready E2E everywhere, NO fakes. Fix in priority order below.

## Confirmed defects (by severity)

### 1. [critical/silent-failure] `crates/ingest/src/pipeline.rs:170`
**Claim:** The ingest pipeline swallows ALL downstream failures (LLM extraction, Neo4j upsert, embedding generation) and returns Ok, so `prism ingest file.csv` exits 0 and prints 'Done.' having parsed the schema but stored NOTHING — live-proven with dead backends (exit 0, 'Done.', Neo4j count unchanged) and the --json output contains zero error indication, so agents cannot detect the failure either.
**Evidence:** pipeline.rs:170-173 `Err(e) => { tracing::error!("LLM extraction failed: {e}"); None }`; same pattern at 192-196 (Neo4j) and 221-224 (embeddings); run_pipeline then returns Ok(IngestResult) at line 230. CLI prints unconditional `println!("\n  Done.")` (crates/cli/src/main.rs:4823) and handle_ingest returns Ok. Live run: `prism ingest test.csv --llm-url http://127.0.0.1:9 ...` -> stdout 'Schema: 3 columns, 2 rows ... Done.', EXIT=0; --json output has no entities/graph/embeddings/error keys at all.
**Fix:** Propagate step failures into IngestResult (e.g. errors: Vec<String> field), print them in print_ingest_summary, include them in --json, and exit non-zero when any configured step failed (schema_only mode exempt).
**Verifier:** CONFIRMED by code reading. crates/ingest/src/pipeline.rs swallows all three downstream failures into None with only tracing::error! (LLM 170-174, Neo4j 192-196, embeddings 221-224) and unconditionally returns Ok(IngestResult) at line 230; IngestResult has no error field. Aggravator beyond the claim: Qdrant upsert failure still returns Some(batch) (214-217), so the CLI reports "Embeddings: N vector

### 2. [critical/broken] `crates/cli/src/main.rs:4617`
**Claim:** Platform-text document ingestion (pdf/json/jsonl/owl/cif/txt/md — the advertised 'PDF/text-like file ingest into the platform knowledge stack', command_tools.rs:246) never ingests the document: PRISM submits each file chunk as an academic SEARCH QUERY, marc27-core searches arXiv/PubMed/S2/OpenAlex with it and refuses to ingest the query text, yet PRISM reports success ('Chunk N: job X [pending]' + 'Done.', tool success:true).
**Evidence:** PRISM submit_platform_ingest_chunk builds `{"source": { "type": "query", "query": chunk }, "mode": "full"}` for chunks up to 48,000 chars (main.rs:4616-4619; line numbers as of mtime 2026-07-06 10:14 — file is being edited concurrently). marc27-core (fix/audit-tail) IngestSource::Query arm at knowledge-service/src/main.rs:1822: 'Search the real academic sources … The previous code ingested the QUERY STRING itself … Honest failure — NEVER fall back to ingesting the query string'; a 48K-char query fails/garbage-matches search_all, so the job fails asynchronously ('no papers found for query …', :1849-1852) — or worse, ingests up to 10 unrelated paper abstracts (QUERY_INGEST_MAX_PAPERS) attributed as arxiv:*/pubmed:* docs. submit_ingest_job (knowledge-service main.rs:1404) accepts any query with 202+job_id, so PRISM's error check passes and run_platform_ingest_file returns jobs as success; the CLI never polls the doomed job. PRISM was written against the old server semantics; the honest server fix orphaned the client contract, and job.rs:9-18 has NO raw-text source variant to migrate to (R2Path requires pre-uploaded text and hard-rejects PDF bytes).
**Fix:** API-first: add IngestSource::Text { doc_id, text } (or a text-upload + R2Path flow) to marc27-core ingest-job, then switch submit_platform_ingest_chunk to it. Until the server supports it, make `prism ingest <textfile>` fail honestly ('platform raw-text ingest not available') instead of submitting query jobs that report submitted-success.
**Verifier:** CONFIRMED by reading both sides. PRISM main.rs:4616-4619 submits each extracted document chunk (up to 48K chars, main.rs:4491) as {"source":{"type":"query","query":chunk},"mode":"full"} to /knowledge/ingest-job; pdf/json/jsonl/owl/cif/txt/md all route here (main.rs:4443-4447) and command_tools.rs:246 advertises it as platform file ingest. marc27-core (fix/audit-tail) knowledge-service/src/main.rs:

### 3. [critical/broken] `PRISM/crates/cli/src/main.rs:4617`
**Claim:** prism ingest of any PDF/text/json/md/owl/cif file submits the document's OWN extracted text as an ingest source of type 'query' — which the knowledge service treats as a web-search string, not as content to ingest. The file's content is never ingested.
**Evidence:** submit_platform_ingest_chunk builds `"source": { "type": "query", "query": chunk }` where `chunk` is up to 48,000 chars of extracted document text (split_text_for_platform_ingest, MAX_CHARS=48_000). On the KS side run_ingest_job's Query arm (knowledge-service/src/main.rs:1822) does `research::web::search_all(&client, query, 10)` — it explicitly does NOT ingest the query text (comment: 'The previous code ingested the QUERY STRING itself... wrote garbage'). So a 48K-char blob of the user's PDF becomes a literal search query against arXiv/Semantic Scholar; the job then ingests at most unrelated papers' abstracts, or fails 'no papers found', while reporting a job_id as if the file was ingested.
**Fix:** Add a raw-text IngestSource variant (e.g. `Text { doc_id, body }`) to core/ingest/job.rs and have run_ingest_job embed+extract that text directly; change submit_platform_ingest_chunk to use it. Until then, prism ingest of documents into the platform is a no-op on the actual file content.
**Verifier:** CONFIRMED at every hop. PRISM CLI main.rs:4617 sends extracted document text (chunks up to 48,000 chars via split_text_for_platform_ingest, main.rs:4490) as {"source":{"type":"query","query":chunk}} for all PlatformText formats (pdf/json/jsonl/owl/cif/txt/md, main.rs:4444-4447). The marc27-core gateway (crates/api/src/routes/knowledge_proxy.rs:277-285) proxies verbatim; KS submit_ingest_job (crate

### 4. [critical/broken] `/Users/siddharthakovid/Downloads/PRISM/crates/cli/src/main.rs:4617`
**Claim:** The entire PRISM document-ingest-to-platform path (`prism ingest x.pdf/txt/md/json/jsonl/owl/cif`, agent ingest/ingest_file tools, watch mode, MCP) never ingests the document: extracted text chunks are submitted as source type 'query', which marc27-core now interprets as an academic-search query, so the user's document content is never stored anywhere while the CLI prints job IDs and 'Done.'
**Evidence:** PRISM main.rs:4617 submit_platform_ingest_chunk: `"source": { "type": "query", "query": chunk }` where chunk is up to 48,000 chars of the user's document text. marc27-core knowledge-service main.rs:1822-1878 IngestSource::Query runs `search_all(&client, query, ...)` against Semantic Scholar/arXiv/etc and explicitly 'NEVER fall back to ingesting the query string' (main.rs:1841-1843). core/src/ingest/job.rs:9-18 has no Text/Document source variant. A 48K-char search query will fail or match garbage; either way the document body is lost. The marc27-core fix that stopped query-string ingestion (correct for real queries) silently broke PRISM's document-push channel, which relied on the old behavior.
**Fix:** Add an IngestSource::Text { doc_id, text } variant to marc27_core::ingest::job (or have PRISM submit chunks via POST /knowledge/embed + /knowledge/graph/ingest which already work), and repoint submit_platform_ingest_chunk at it. Add an end-to-end test: prism ingest a .txt -> assert chunk appears in /knowledge/search results.
**Verifier:** Verified real on both repos. PRISM main.rs:4617 submits every extracted document chunk (up to 48,000 chars, MAX_CHARS at :4491) as {"source":{"type":"query","query":chunk}} to /knowledge/ingest-job; all pdf/txt/md/json/jsonl/owl/cif files route there via ingest_backend (main.rs:4444-4448) → run_platform_ingest_file (:4703, :4728), and the same path serves the agent ingest/ingest_file/ingest_watch 

### 5. [high/fake] `crates/ingest/src/pipeline.rs:216`
**Claim:** When the Qdrant upsert fails, the pipeline still returns the embedding batch as if stored, and the CLI prints 'Embeddings: N vectors (dim=D)' — output indistinguishable from a successful store, so the user believes vectors are in Qdrant when none were written.
**Evidence:** pipeline.rs:214-217 `Err(e) => { tracing::error!("Qdrant upsert failed: {e}"); Some(batch) // still return embeddings even if store failed }`; CLI then prints the embeddings line whenever result.embeddings is Some (crates/cli/src/main.rs:4758-4768).
**Fix:** Distinguish 'generated' from 'stored' in IngestResult (e.g. stored_count: usize or a store_error field) and have the CLI say 'generated N vectors, STORE FAILED: <err>' when the upsert failed.
**Verifier:** Confirmed real. pipeline.rs:214-217 returns Some(batch) on Qdrant upsert Err (comment: "still return embeddings even if store failed"); success and failure arms are indistinguishable in IngestResult (no stored flag, fields at pipeline.rs:30-38), and ingest_file returns Ok regardless. CLI serializes the result unmarked (main.rs:4694-4699) and prints "Embeddings: {count} vectors (dim={dimension})" w

### 6. [high/broken] `crates/llm/src/lib.rs:364`
**Claim:** Local entity extraction is functionally broken on PRISM's flagship default local stack (llama.cpp + Gemma thinking model): generate_json hardcodes max_tokens 4096 with no reasoning-model handling, the model burns all 4096 tokens in reasoning_content and emits empty content (finish_reason=length), extract_content falls back to the non-JSON reasoning text, parsing fails on every ingest — and finding #1 converts that into permanent fake success.
**Evidence:** Live: exact extraction prompt to the running gemma-4-12B llama.cpp server returned content len 0, reasoning len 11000, finish_reason 'length', usage completion_tokens 4096; prism ingest logged 'LLM extraction failed: LLM returned invalid extraction JSON' yet printed 'Done.' EXIT=0. Code: generate_json body `"max_tokens": 4096` (lib.rs:372) and extract_content reasoning_content fallback (lib.rs:233-243). Also the default 120s timeout (crates/core/src/config.rs:153-155 default_llm_timeout) aborts extraction on this hardware before the model finishes, producing the same silent outcome.
**Fix:** For extraction: raise/omit max_tokens or budget via config, disable thinking (chat_template kwargs / reasoning_effort none) for generate_json, never treat reasoning_content as JSON output, and surface finish_reason=length as a distinct actionable error.
**Verifier:** CONFIRMED by code reading. The cited path is exactly what runs on ingest: crates/ingest/src/lib.rs:23 re-exports prism_llm as crate::llm, so ontology.rs:65 calls generate_json at crates/llm/src/lib.rs:364-378, which hardcodes "max_tokens": 4096 (lib.rs:372) with no reasoning-model handling — no finish_reason check, no retry, no thinking-disable request param, and no config knob (max_tokens exists 

### 7. [high/silent-failure] `crates/ingest/src/pipeline.rs:216`
**Claim:** The local tabular pipeline soft-fails every downstream step (LLM extraction, Neo4j upsert, Qdrant upsert) — errors are logged then discarded and run_pipeline returns Ok, so `prism ingest data.csv` with all backends down still exits 0, prints 'Done.', and the ingest_file tool returns success:true while NOTHING was stored; a failed Qdrant write even returns the batch as if stored.
**Evidence:** pipeline.rs:170-174 `Err(e) => { tracing::error!("LLM extraction failed: {e}"); None }`; :192-196 same for `Neo4j upsert failed`; :214-217 `Err(e) => { tracing::error!("Qdrant upsert failed: {e}"); Some(batch) // still return embeddings even if store failed }` — the IngestResult then claims embeddings exist (print_ingest_summary prints 'Embeddings: N vectors', cli/main.rs:4818-4830) and execute_cli_command reports success:true on exit 0 (command_tools.rs:2758). The only tell is absent JSON keys — the human summary, exit code, and agent tool result all say success.
**Fix:** Propagate step errors (or collect per-step {ok, error} statuses into IngestResult and make the CLI exit non-zero / tool report success:false when any configured step failed). At minimum never return Some(batch) after a failed vector-store write.
**Verifier:** CONFIRMED by reading the code. pipeline.rs run_pipeline (crates/ingest/src/pipeline.rs:125-240) swallows all three downstream failures: LLM extraction Err → logged+None (:170-174), Neo4j upsert Err → logged+None (:192-195), Qdrant upsert Err → logged + `Some(batch) // still return embeddings even if store failed` (:214-217), then unconditionally returns Ok(IngestResult) (:230); IngestResult has no

### 8. [high/fake] `PRISM/app/tools/memory/tool.py:147`
**Claim:** search_artifacts/fetch_artifact tell the agent every tool output is auto-indexed, but artifact recording is OFF by default since the 2026-07 one-memory-spine consolidation — nothing new is ever written to artifacts.db
**Evidence:** _RECALL_DESCRIPTION: "Every meaningful tool output from this and prior sessions has been auto-indexed" and _FETCH_SCHEMA: "auto-injected as _artifact_id on tool outputs"; but app/plugins/bootstrap.py:113-117 sets record_enabled only when PRISM_ARTIFACT_RECORDING is truthy, nothing in the repo sets it (grep: only bootstrap.py mentions it), and recorder.py:244 `if store is None or not _CONFIG["record_enabled"]: return result` short-circuits all recording
**Fix:** Rewrite the three tool descriptions to state the store is a read-only historical archive by default (point live recall at the Rust provenance spine), and have _recall include a `recording: disabled` field so the agent knows hits only cover pre-consolidation artifacts
**Verifier:** Confirmed by tracing the real execution path. (1) The stale promises ship to the agent: search_artifacts is registered with "Every meaningful tool output from this and prior sessions has been auto-indexed" (app/tools/memory/tool.py:145-159, registered tool.py:284-290) and fetch_artifact's schema claims artifact_id is "auto-injected as _artifact_id on tool outputs" (tool.py:212-218); app/tool_serve

### 9. [high/silent-failure] `PRISM/app/tools/data_collectors/collector.py:112`
**Claim:** MPCollector swallows every runtime failure (invalid key, network, API change) and returns [], indistinguishable from 'Materials Project has no matching materials'
**Evidence:** collect() raises CollectorConfigError for a MISSING key (the audited honesty fix, line 90 — still intact), but the very next block ends `except Exception:\n            return []` (lines 112-113), so acquire_materials' `skipped` reporting (acquisition.py:72) never fires for real failures
**Fix:** Drop the blanket except or re-raise as a tagged error so acquisition.py records the skip; only return [] for genuinely-empty search results
**Verifier:** Verified real. MPCollector.collect() (PRISM/app/tools/data_collectors/collector.py:94-113) wraps the entire MPRester block — including the mp_api import at line 95 — in a try whose handler is bare `except Exception: return []` (lines 112-113), with no logging. Only the missing-key case raises CollectorConfigError (lines 90-93). The caller _acquire_materials (PRISM/app/tools/skills/acquisition.py:6

### 10. [high/silent-failure] `PRISM/app/tools/data_collectors/patent_collector.py:64`
**Claim:** PatentCollector swallows Lens API failures (401/429/5xx after raise_for_status) and returns [], contradicting its own docstring and prior_art_search's advertised `patents_error` reporting
**Evidence:** Docstring lines 21-23: "Raising lets prior_art_search report `patents_error`" — true only for the missing-token path; the request path ends `except Exception:\n            return []` (lines 64-65) so resp.raise_for_status() errors never propagate, and search.py:146-148 promises "Per-source failures (e.g. Lens auth missing) are reported in papers_error / patents_error" — false for every API failure
**Fix:** Remove the try/except (or catch only to re-raise with context) so _prior_art_search's except at search.py:111 populates patents_error as designed
**Verifier:** CONFIRMED by reading the live code on PRISM main (no fix landed; last touches to the file are 51f5260d/a56d8229 and the working tree is clean). The entire Lens request path in PatentCollector.collect() — patent_collector.py:28-63, including resp.raise_for_status() at line 40 — is wrapped in a bare `except Exception: return []` at lines 64-65. So HTTP 401 (bad token), 429, 5xx, timeouts, and JSON d

### 11. [high/silent-failure] `PRISM/app/tools/data_collectors/collector.py:44`
**Claim:** OPTIMADECollector returns [] on ANY failure — optimade lib missing (line 33-34) or whole-federation query failure (line 44-45) — so agents read 'no materials found' when the source errored
**Evidence:** `except ImportError:\n            return []` and `try: client = OptimadeClient(...); raw = client.get(filter_string)\n        except Exception:\n            return []`; also _get_fallback_providers (line 17-18) silently returns [] leaving the collector with zero providers
**Fix:** Raise CollectorConfigError for ImportError/no-providers and let total query failure propagate so acquisition.py surfaces it in `skipped`; keep per-provider partial tolerance inside OptimadeClient
**Verifier:** CONFIRMED by reading the code. OPTIMADECollector.collect (/Users/siddharthakovid/Downloads/PRISM/app/tools/data_collectors/collector.py) returns [] on `except ImportError` (lines 33-34, optimade lib not installed) and on `except Exception` (lines 44-45, whole-federation query failure: network down, all providers erroring, bad filter). _get_fallback_providers (lines 17-18) also returns [] on any re

### 12. [high/broken] `crates/core/src/ontology/sources/ntrs.rs:65`
**Claim:** NTRS source sweep pagination is entirely non-functional: the connector sends 'page' and 'size' query params which the live NTRS API silently ignores (real params are 'page.from'/'page.size'), so every page returns the same first ~10 results
**Evidence:** .query(&[("q", query), ("page", &page.to_string()), ("size", &size.to_string())]) — live-verified: curl with page=0,1,2&size=2 all return the identical 10 IDs [19660014185, …], while page.from/page.size paginate correctly. pipeline::ingest_source (pipeline.rs:224-278) loops until max_docs since the page is never empty, so an NTRS sweep with max_docs=100 re-ingests the same ~10 docs 10x, burns 10x the LLM extraction budget, and reports docs=100 (fake progress; MERGE upserts hide the duplication in the store)
**Fix:** Send page.from=(page*size) and page.size=size; also reconcile the 0-based ntrs::search doc with pipeline's 1-based page=1 start; add a live smoke that asserts page 2 differs from page 1
**Verifier:** CONFIRMED, not refuted (note: file is in marc27-core, not PRISM — /Users/siddharthakovid/Downloads/marc27-core/crates/core/src/ontology/sources/ntrs.rs). (1) Code verified: ntrs.rs:65-69 sends .query(&[("q", query), ("page", ...), ("size", ...)]). (2) Live re-verified 2026-07-06: GET /api/citations/search?q=titanium%20alloy with page=1,2,3&size=2 all return the identical 10 IDs [19670002466, 19650

### 13. [high/silent-failure] `crates/knowledge-service/src/main.rs:1784`
**Claim:** IngestSource::Url advertises 'URL to a PDF, CSV, JSON, or other file' but a PDF URL is lossy-decoded to garbage text, sent to the LLM extractor, embedded, and reported as a Completed success — unlike the R2Path branch which honestly rejects %PDF
**Evidence:** match resp.text().await { Ok(text) … let cleaned = if is_html || _looks_like_html(&text) { … } else { text }; vec![(url.clone(), cleaned)] } — no bytes.starts_with(b"%PDF") guard (the R2Path branch at main.rs:1895 has exactly this guard with an honest failure message); job.rs:10-11 documents Url as accepting PDFs
**Fix:** Check Content-Type/application/pdf and the %PDF magic on the fetched body and fail the job with the same honest message the R2Path branch uses
**Verifier:** CONFIRMED. In marc27-core crates/knowledge-service/src/main.rs, the IngestSource::Url branch (1726-1821) checks content-type only for text/html (1778-1783) and lossy-decodes the body via resp.text().await at 1784 — reqwest never errors on invalid UTF-8, so a PDF URL becomes replacement-char garbage that passes through as `cleaned` (1800-1803). The garbage is sent to the LLM extractor (2017), chunk

### 14. [high/silent-failure] `crates/core/src/embeddings/search.rs:105`
**Claim:** Chunk upsert key omits tenant — MERGE on (doc_id, chunk_idx) means two tenants ingesting the same document (global doc_ids like 'arxiv:2301.12345' or a shared URL) silently overwrite each other's chunk, flipping c.tenant to the last writer so the first tenant's data vanishes from their search results
**Evidence:** MERGE (c:Chunk {doc_id: $doc_id, chunk_idx: $chunk_idx}) SET … c.tenant = $tenant — tenant is SET, not part of the MERGE key; doc_ids are client-supplied on /embed and deterministic cross-tenant on /ingest-job Query and /ingest/source (query_papers_to_texts produces 'arxiv:…' ids for every tenant)
**Fix:** Include tenant in the MERGE key: MERGE (c:Chunk {doc_id: $doc_id, chunk_idx: $chunk_idx, tenant: $tenant})
**Verifier:** CONFIRMED real. crates/core/src/embeddings/search.rs:105-111 MERGEs Chunk on (doc_id, chunk_idx) only; tenant, corpus_id, content, metadata, and embedding are all in SET, so the last writer clobbers the node. All tenants share one FalkorDB graph (FALKORDB_GRAPH, default "marc27" — crates/core/src/graph/client.rs:48); isolation is property-based only. Colliding doc_ids across tenants are real: /emb

### 15. [high/silent-failure] `crates/core/src/graph/ingest.rs:64`
**Claim:** Entity upsert key omits tenant — MERGE on name_lower alone means any two tenants extracting the same entity name (e.g. 'Titanium') share one node whose tenant flips to the last writer, silently removing it from the earlier tenant's tenant-filtered graph queries
**Evidence:** MERGE (e:Entity {name_lower: $name_lower}) SET … e.tenant = $tenant — while graph_search/get_neighbors filter WHERE tenant IN $tenants; create_relationship (line 100) has the same last-writer-wins r.tenant. graph_ingest handler (main.rs:646-653) carefully overrides tenant from auth, but the store then discards that isolation on collision
**Fix:** Add tenant to the Entity MERGE key (and relationship MERGE), or model tenancy as separate labels/graphs
**Verifier:** CONFIRMED by reading the live code on fix/audit-tail. ingest.rs:64-66 MERGEs Entity on {name_lower} only and sets e.tenant (plus e.props/label/entity_type) in the SET clause — last writer wins on cross-tenant name collision. GraphClient (client.rs:48,86) uses ONE shared FalkorDB graph (FALKORDB_GRAPH, default "marc27") for all tenants, and derive_tenant (knowledge-service/main.rs:1341) issues dist

### 16. [high/broken] `crates/knowledge-service/src/main.rs:2838`
**Claim:** admin_embed_graph pairs each stored embedding with the WRONG content once any single store fails: content is indexed by the global success counter (embedded % 20) instead of the row's position in the current batch, and can panic (index out of bounds) on the final short batch
**Evidence:** content: &texts[embedded % 20] inside for ((name, etype, _label), result) in chunk.iter().zip(results.iter()) — after one store_embedding Err (embedded not incremented, loop continues) every subsequent chunk row stores another row's text as its content; with a trailing batch smaller than 20, a prior failure shifts the index past texts.len() and panics the background task, silently killing the whole re-embed job
**Fix:** Iterate with enumerate and use texts[i] (the zip already aligns name/result/text by position)
**Verifier:** CONFIRMED at marc27-core crates/knowledge-service/src/main.rs:2838 (claim said PRISM but the file is in marc27-core; line matches exactly). `embedded` (line 2821) is a job-global success counter never reset per chunk; line 2838 uses `content: &texts[embedded % 20]` where `texts` is the CURRENT chunk's per-chunk Vec. On any `store_embedding` Err (line 2850) `embedded` is not incremented and the loo

### 17. [high/silent-failure] `crates/knowledge-service/src/main.rs:1409`
**Claim:** POST /ingest-job accepts jobs and marks them Completed having stored NOTHING when FALKORDB_URL is unset: submit only gates on llm_service_url, then the worker silently skips graph writes (if let Some(ref g) = graph) and embeddings (if let (Some(emb), Some(vdb))), still spending LLM extraction money per source
**Evidence:** submit_ingest_job checks only state.llm_service_url (main.rs:1409-1414); run_ingest_job calls extract_entities unconditionally (2017) then 'if let Some(ref g) = graph {' (2047) and '(Some(ref emb), Some(ref vdb))' (2098) — both silently no-op when None — and unconditionally sets IngestJobStatus::Completed (2217)
**Fix:** Reject job submission (503, honest 'graph/vector store not configured') when the stores required by the requested mode are absent, matching how llm_url is already gated
**Verifier:** CONFIRMED by reading the code. Precondition real: knowledge-service starts with graph=None when FALKORDB_URL is unset (core/src/graph/client.rs:40-46 returns Ok(None)) or when the connection fails (main.rs:3205-3210 warns and continues); vectors_db = graph.clone() (3223). submit_ingest_job gates ONLY on llm_service_url (main.rs:1409-1414) and returns 202 — no graph/vectors check, even though five 

### 18. [high/silent-failure] `/Users/siddharthakovid/Downloads/PRISM/crates/ingest/src/pipeline.rs:192`
**Claim:** Local tabular ingest swallows every downstream failure — LLM extraction, Neo4j upsert, and Qdrant upsert errors are logged at tracing level and converted to None, so `prism ingest data.csv` exits 0 and prints 'Done.' when nothing was stored.
**Evidence:** pipeline.rs:170-173 `Err(e) => { tracing::error!("LLM extraction failed: {e}"); None }`; :192-195 same for Neo4j upsert; :214-217 `Err(e) => { tracing::error!("Qdrant upsert failed: {e}"); Some(batch) // still return embeddings even if store failed }`. run_pipeline returns Ok(IngestResult) regardless; CLI print_ingest_summary (main.rs:4747-4769) merely omits the Graph/Embeddings lines and always prints '\n  Done.' with exit code 0.
**Fix:** Return Err (or set a failed/partial flag on IngestResult that the CLI turns into a non-zero exit and an explicit 'graph write FAILED: <err>' line) when a configured backend write fails; only omit sections for backends that were deliberately unconfigured.
**Verifier:** CONFIRMED by reading the real execution path. (1) /Users/siddharthakovid/Downloads/PRISM/crates/ingest/src/pipeline.rs:170-173 — LLM extraction Err → tracing::error! + None, pipeline continues; :192-195 — Neo4j upsert Err → tracing::error! + None; :214-217 — Qdrant upsert Err → tracing::error! + Some(batch) with comment "still return embeddings even if store failed". run_pipeline returns Ok(Ingest

### 19. [high/silent-failure] `/Users/siddharthakovid/Downloads/marc27-core/crates/knowledge-service/src/main.rs:2047`
**Claim:** run_ingest_job marks the job Completed even when the graph store is not configured (all graph writes silently skipped), when batch_ingest fails for every source, or when extraction fails for every source — the only refusal check at submit time is for the LLM URL.
**Evidence:** submit_ingest_job (main.rs:1409-1414) only errors when llm_service_url is None. In run_ingest_job, graph writes happen only `if let Some(ref g) = graph` (main.rs:2047) with no else and errors reduced to `tracing::warn!` (main.rs:2056-2058); per-source extraction failures are warnings (main.rs:2196-2204); completion at main.rs:2219-2230 unconditionally sets IngestJobStatus::Completed with progress 1.0. A deployment without FALKORDB_URL accepts jobs, burns LLM tokens on extraction, stores nothing, and reports Completed.
**Fix:** Refuse submission when graph AND vectors are both unconfigured for the requested mode; mark the job Failed (or PartialFailure with per-source errors) when zero entities/embeddings were stored or all sources errored.
**Verifier:** CONFIRMED by reading the code on fix/audit-tail. Every element of the claim holds: (1) submit-time refusal only checks the LLM URL — crates/knowledge-service/src/main.rs:1409-1414 errors 503 only when state.llm_service_url is None; state.graph being None is not checked before accepting the job. (2) The no-graph deployment is a real, reachable state: at startup (main.rs:3205-3208) a failed/absent F

### 20. [medium/gap] `crates/ingest/src/graph_validation.rs:60`
**Claim:** The SHACL-lite graph quality validator (orphan relationships, invalid types, empty names, duplicates) is advertised as running 'before/after writing to Neo4j' but is never called anywhere — LLM extraction output is written to Neo4j completely unvalidated.
**Evidence:** Module docstring: 'Validates structural integrity of the knowledge graph before/after writing to Neo4j'. `grep -rn validate_graph crates/` finds only the definition and `pub mod graph_validation` in lib.rs:19 — zero callers; pipeline.rs Step 4 passes the raw EntitySet straight to Neo4jGraphStore::upsert.
**Fix:** Call validate_graph(entity_set) between extraction and the Neo4j upsert in run_pipeline; block on Error-severity issues, attach the report to IngestResult.
**Verifier:** CONFIRMED — the defect is real. (1) `validate_graph` is defined at /Users/siddharthakovid/Downloads/PRISM/crates/ingest/src/graph_validation.rs:60 with a module docstring (lines 1-10) advertising "Validates structural integrity of the knowledge graph before/after writing to Neo4j". (2) A repo-wide grep of both PRISM and marc27-core for `validate_graph`/`graph_validation`/`GraphValidationReport` fi

### 21. [medium/gap] `crates/ingest/src/mapping.rs:97`
**Claim:** --mapping promises 'custom entity/relationship rules' but the deterministic rule machinery (match_column, should_ignore, expand_alias) is never called by anything — the YAML is only pasted into the LLM prompt as advisory hints, so ignore_columns are still sent to the LLM and aliases are never applied to extracted entities.
**Evidence:** grep across all crates: match_column/should_ignore/expand_alias have zero callers outside mapping.rs tests. The only pipeline use is ontology.rs:126-128 `prompt.push_str(&m.to_prompt_supplement())`. CLI help (main.rs:218-220) calls it 'custom entity/relationship rules'.
**Fix:** Either enforce the rules post-extraction (drop ignored columns before sampling, apply expand_alias to entity names, validate types against entity_rules) or re-document --mapping as prompt hints only.
**Verifier:** Verified real. The only production use of OntologyMapping is ontology.rs:127 `prompt.push_str(&m.to_prompt_supplement())` (reached via pipeline.rs:159 → build_extraction_prompt_with_mapping at ontology.rs:126-128). Grep across all crates confirms match_column (mapping.rs:97), should_ignore (mapping.rs:89), and expand_alias (mapping.rs:107) have zero callers outside mapping.rs's own test module — t

### 22. [medium/silent-failure] `crates/cli/src/main.rs:5065`
**Claim:** Watch mode silently ignores subdirectories: both the initial scan and the 5s poll loop use non-recursive read_dir, while one-shot directory ingest recurses — files landing in any subfolder of the watched directory are never ingested and no warning is emitted.
**Evidence:** handle_ingest_watch initial scan `for entry in std::fs::read_dir(dir)?` (main.rs:5065) and poll loop `std::fs::read_dir(dir)` (main.rs:5104) only pass `is_ingestable(&path)` files (path.is_file() required, dirs skipped); contrast collect_ingest_paths (main.rs:4396-4425) which explicitly recurses with a stack for the non-watch path.
**Fix:** Recurse in the watch scan (reuse collect_ingest_paths) or print an explicit 'subdirectories are not watched' notice at startup.
**Verifier:** Defect verified in /Users/siddharthakovid/Downloads/PRISM/crates/cli/src/main.rs. Watch mode (handle_ingest_watch, main.rs:5096) uses non-recursive std::fs::read_dir in both the initial scan (main.rs:5128) and the 5s poll loop (main.rs:5167), filtering with is_ingestable (main.rs:5220-5222) which requires path.is_file() — subdirectory entries are silently `continue`d with no recursion and no warni

### 23. [medium/silent-failure] `crates/agent/src/command_tools.rs:2747`
**Claim:** On command-tool timeout the result tells the agent the process 'is still running' — but kill_on_drop(true) kills it the moment the timed-out future drops; for ingest_watch (advertised 'Watch a directory and continuously ingest', spec :260-267) this means watching silently STOPS after ≤300s while the agent believes it continues.
**Evidence:** command_tools.rs:2733 `.kill_on_drop(true)`; :2737 `timeout(timeout_window, cmd.output())` — on Err the child future is dropped and the process SIGKILLed; :2747 returns stderr `"`{invocation}` is still running after {timeout_secs} seconds. Run the command directly…"`. command_timeout_for_root("ingest") = 300s (:1823-1826). handle_ingest_watch (cli/main.rs:5096) is an infinite poll loop, so the ingest_watch tool ALWAYS ends this way: initial-scan files are really ingested, then the watcher dies while the result claims it is still running (and the hint violates the no-exit-to-CLI rule).
**Fix:** Change the timeout message to state the process WAS terminated. For ingest_watch, either remove it from the agent catalog or make it start a node-managed background watcher and return the watcher id honestly.
**Verifier:** Defect confirmed by tracing the real path. ingest_watch (spec command_tools.rs:259-267) maps via CommandToolKind::IngestWatch (:2025-2028, '--watch' added in build_ingest_args :1911) to CommandExecution::Cli, dispatched at :2966-2967 into execute_cli_command (:2716). That function sets .kill_on_drop(true) (:2733) and wraps cmd.output() in timeout() (:2737) with command_timeout_for_root("ingest")=3

### 24. [medium/gap] `crates/server/src/handlers/data.rs:56`
**Claim:** POST /api/data/ingest — the node's only dedicated HTTP ingestion endpoint, already RBAC-gated with Permission::IngestData (router.rs:102-107) — is an honest 501 that points callers at the CLI; the 501 is still accurate (nothing behind it), but for 'ingest documents/data from any node, anytime' the direct HTTP document-upload path is core and unwired (the real HTTP route is the non-obvious POST /api/tools/ingest_file/run with {"approve":true}).
**Evidence:** data.rs:56-68 returns StatusCode::NOT_IMPLEMENTED with message 'Ingestion is not available on this endpoint … Use `prism ingest <file>` or the knowledge-service ingest API.' — no queue, no pipeline, and the `format` request field (:21) is parsed and unused.
**Fix:** Wire it to the same executor ingest_file uses (accept a node-local path or inline text/base64 payload → prism-ingest pipeline or knowledge-service job), keeping the existing IngestData RBAC; keep 501 only for still-unsupported payload kinds.
**Verifier:** Verified in code. PRISM data.rs:56-68 returns an honest 501 (no State, no queue, no pipeline) with the exact quoted message; the `format` field (data.rs:21) is parsed and never used; router.rs:103-107 gates POST /api/data/ingest with Permission::IngestData and it is the node's only dedicated HTTP ingest route. The claimed real path checks out end-to-end: POST /api/tools/ingest_file/run (router.rs:

### 25. [medium/gap] `crates/workflows/src/lib.rs:830`
**Claim:** No workflow step or remote-relay caller can ingest a local document at all: run_tool_step's body carries only command+inputs (no way to pass the approve flag run_tool reads from the body root), and the relay hardcodes approve=false — so approval-gated ingest/ingest_file/ingest_watch are honestly 422-refused on both paths, leaving knowledge_ingest (url/query only, no local files) as the sole remotely-invocable ingestion tool.
**Evidence:** workflows/src/lib.rs:830-833 `json!({"command": command, "inputs": inputs})`; server handlers/tools.rs:160-163 reads `approve` from the body root (and build_tool_args strips it from bare args, so it cannot ride through inputs); cli/main.rs:2372 relay executor calls `.invoke_tool(&req.tool, req.args, Some(caller), false)`; service.rs:298-304 then refuses gated tools. ingest/ingest_file/ingest_watch are all requires_approval:true (command_tools.rs:248,257,266); knowledge_ingest is requires_approval:false FullAccess (:607-608) but takes only url|query.
**Fix:** Owner decision: either an owner-configured allowlist of workflow/relay-approvable tools (workflow spec could carry approve: [...] which the CLI translates to the body-root flag), or a dedicated non-gated, path-sandboxed ingest-from-path tool for workflow/relay use.
**Verifier:** Every cited fact checks out in PRISM (branch main), and all escape hatches I hunted for are closed. (1) Workflow tool steps cannot approve: crates/workflows/src/lib.rs:830-833 builds the body as exactly {"command", "inputs"} — the only config keys read are name/tool (:788), command/cmd (:794) and inputs/args/with/params (:802); there is no way to place a root-level "approve". (2) The server reads 

### 26. [medium/gap] `PRISM/app/tools/data_collectors/omat24_collector.py:15`
**Claim:** On a default install the omat24 source ALWAYS silently returns [] because `datasets` only ships in the optional [data] extra, and the ImportError is swallowed
**Evidence:** `try:\n            from datasets import load_dataset\n        except ImportError:\n            return []` (lines 13-16); pyproject.toml declares datasets>=3.0.0 only under [project.optional-dependencies].data
**Fix:** Raise CollectorConfigError("omat24 requires the 'data' extra — pip install 'prism-platform[data]'") like the MP/Lens fixes
**Verifier:** Defect confirmed by reading the code. omat24_collector.py:13-16 swallows ImportError and returns [], and pyproject.toml declares datasets>=3.0.0 only in the [data] optional extra (base dependencies at pyproject.toml:34-58 omit it; install.sh:189 installs prism-platform with no extras). The datasets import is deferred inside collect(), so the collector registers successfully on default installs (ba

### 27. [medium/silent-failure] `PRISM/app/tools/memory/embedder.py:158`
**Claim:** On every standard install the 'semantic vector' half of search_artifacts recall is a hash embedder that is explicitly NOT semantic — sentence-transformers is not declared anywhere in pyproject.toml and is absent from the venv — and the agent-facing result never says which backend ran
**Evidence:** get_default_embedder falls back to HashEmbedder (docstring: "NOT semantic ... two semantically-similar inputs will be unrelated in vector space") with only a log warning; `grep sentence pyproject.toml` → no matches (base deps and all extras); tool.py:145 still advertises "Hybrid recall (BM25 keyword + semantic vector, RRF-fused)"; hash vectors are also what gets stored in artifacts.db when recording is on
**Fix:** Add sentence-transformers to an extra + declare it, and include an `embedder: "hash (non-semantic fallback)"` field in _recall output so ranking quality is honest to the agent
**Verifier:** CONFIRMED — the defect is real and every refutation avenue is closed. (1) Fallback is real: app/tools/memory/embedder.py:158-168 — get_default_embedder() tries `import sentence_transformers` and on ANY exception logs only a WARNING and returns HashEmbedder(), whose own docstring (embedder.py:46-52) says "NOT semantic ... two semantically-similar inputs will be unrelated in vector space". (2) Stand

### 28. [medium/broken] `PRISM/app/tools/session_context.py:96`
**Claim:** session_context's advertised disk persistence corrupts itself: Python sets are serialized via default=str, so after any process restart action='record' crashes and action='status' returns character-garbage
**Evidence:** _fresh_session uses `"objectives_seen": set()` / `"verifiers_used": set()` (lines 96-97); _save_session does `json.dumps(session, indent=2, default=str)` (line 78) writing e.g. "set()" as a string; on reload _record_evaluation calls `.add()` on that string (line 137) → AttributeError, and status does `list("set()")` → ['s','e','t','(',')']; contradicts the tool description "persists to disk (~/.prism/sessions/) so it survives history compaction"
**Fix:** Store them as lists with membership checks (or convert set↔list in _save_session/_load_session)
**Verifier:** Defect verified by reading code and reproducing the round-trip. _fresh_session uses Python sets for objectives_seen/verifiers_used (session_context.py:96-97); _save_session serializes with json.dumps(..., default=str) (line 78) which writes the sets as strings ("set()" / "{'delta'}" — empirically confirmed); _load_session (line 64) does plain json.loads with no rehydration, so after any process re

### 29. [medium/silent-failure] `PRISM/app/tools/web.py:119`
**Claim:** web(action='read') fallback fetch never checks the HTTP status, so a 404/500 error page is returned as the page's 'content' in a success-shaped result — the agent ingests error HTML as document text
**Evidence:** `r = httpx.get(url, timeout=15, follow_redirects=True, ...)` (lines 119-126) is followed directly by BeautifulSoup extraction and `return {"url": url, ..., "content": text, "source": "basic_fetch"}` — no r.raise_for_status() and no status_code in the result (the Firecrawl primary path does raise_for_status)
**Fix:** Call r.raise_for_status() (or include status_code and return an error dict for >=400) before extracting text
**Verifier:** CONFIRMED by reading /Users/siddharthakovid/Downloads/PRISM/app/tools/web.py. The basic-fetch fallback in _web_read does `r = httpx.get(url, timeout=15, follow_redirects=True, ...)` at lines 119-126 and goes straight to BeautifulSoup/regex extraction (lines 128-148) and a success-shaped return {"url", "title", "content", "source": "basic_fetch", "content_length"} at lines 150-156. There is no r.ra

### 30. [medium/fake] `crates/core/src/ingest/job.rs:30`
**Claim:** ExtractionMode is mostly ignored: mode=Raw ('just store raw text, no extraction') runs LLM extraction and graph writes anyway and never stores any raw text; mode=Embed also runs LLM extraction + graph ingest the caller didn't ask for
**Evidence:** run_ingest_job calls extract_entities for every text regardless of req.mode (main.rs:2017) and batch_ingest whenever graph is configured (main.rs:2047); the only mode check is matches!(req.mode, Full | Embed) gating embeddings (main.rs:2093). No branch stores raw text anywhere for Raw
**Fix:** Gate extraction+graph on Graph|Full, implement Raw (store text to R2/chunks without extraction) or remove Raw and Embed's extraction side effect
**Verifier:** Confirmed by reading the real execution path. ExtractionMode (marc27-core crates/core/src/ingest/job.rs:23-32) promises Raw = "just store raw text, no extraction", but run_ingest_job (crates/knowledge-service/src/main.rs:1623) reads req.mode in exactly one place: the matches!(Full|Embed) embed gate at main.rs:2093-2097. extract_entities (LLM call) runs for every text unconditionally (main.rs:2017)

### 31. [medium/gap] `crates/core/src/ingest/job.rs:45`
**Claim:** IngestJobRequest.budget_max_usd is accepted from clients but never read anywhere — there is no budget enforcement on ingest jobs, so a job burns unbounded LLM tokens regardless of the cap the client set
**Evidence:** grep for budget_max_usd across knowledge-service and core/ingest shows only the field declaration (job.rs:45); run_ingest_job never references it while looping extract_entities over all sources
**Fix:** Track cost per extraction call in the loop and fail/stop the job when accumulated cost exceeds budget_max_usd, or remove the field
**Verifier:** CONFIRMED. IngestJobRequest.budget_max_usd (crates/core/src/ingest/job.rs:45, documented "Max budget for this job in USD") is accepted via GraphQL submitIngestJob (crates/api/src/routes/graphql/ingest.rs:78,213) and deserialized by the knowledge-service handler (crates/knowledge-service/src/main.rs:1407), but run_ingest_job (main.rs:1623-2226) destructures req and reads only source/mode/llm_model 

### 32. [medium/silent-failure] `crates/knowledge-service/src/main.rs:2217`
**Claim:** An ingest job whose EVERY source failed extraction (or whose graph writes all errored) still ends status=Completed with progress 1.0 — graph ingest errors and chunk-store failures go to tracing::warn only, and the failure branch merely appends a log line without affecting status
**Evidence:** Err(e) => { tracing::warn!(…"extraction failed for source"); job.log(format!("Warning: extraction failed…")) } (main.rs:2186-2197) then unconditional job.status = IngestJobStatus::Completed (2217); batch_ingest Err at 2057-2059 is tracing::warn only (not even job.log)
**Fix:** Track per-source failure count on the job; set status=Failed (or a partial status + error field) when zero sources succeeded, and job.log graph/embed store errors so the client-visible log is honest
**Verifier:** CONFIRMED by reading marc27-core/crates/knowledge-service/src/main.rs (branch fix/audit-tail). Per-source extraction Err (2186-2197) only does tracing::warn + job.log — IngestJob::log (core/src/ingest/job.rs:101-105) just appends a string; job.fail() is never called in the loop. batch_ingest Err (2052-2060) is tracing::warn only (no job.log). Chunk embed/store failures (2144-2162) are warn-only to

### 33. [medium/gap] `crates/knowledge-service/src/main.rs:59`
**Claim:** Ingest jobs live only in an in-process HashMap: a service restart/redeploy kills running jobs mid-flight and evaporates all job records (client polls get 404 for a job that was really running), the map grows unboundedly (no eviction), and any multi-replica deployment breaks status polling
**Evidence:** ingest_jobs: std::sync::Arc<tokio::sync::RwLock<HashMap<Uuid, IngestJob>>> (main.rs:59-62), inserted at 1427, work spawned with tokio::spawn (1446) — no Postgres persistence, no TTL/eviction anywhere
**Fix:** Persist jobs to Postgres (create row on submit, update status/counters from the worker), or at minimum evict completed jobs after a TTL and document single-replica constraint
**Verifier:** CONFIRMED on branch fix/audit-tail — no fix landed. Verified by reading /Users/siddharthakovid/Downloads/marc27-core/crates/knowledge-service/src/main.rs: (1) `ingest_jobs: Arc<RwLock<HashMap<Uuid, IngestJob>>>` is a field of the in-process KnowledgeState (main.rs:59-61), initialized empty at startup (main.rs:3269). (2) submit_ingest_job inserts the job only into this map (main.rs:1427) and runs t

### 34. [medium/silent-failure] `crates/knowledge-service/src/main.rs:2203`
**Claim:** Ingest-job metering is triple-broken: the record call passes user_id where project_id belongs, records a flat 1.0 usage (the computed token cost never reaches billing), and its error is discarded with let _ = — a failed meter write is invisible
**Evidence:** let _ = marc27_core::metering::usage::record_job_usage(&db, job_id, "knowledge_ingest", user_id, user_id /* project_id fallback */, None) (main.rs:2203-2211); record_job_usage records quantity 1.0 with no cost parameter (metering/usage.rs:12-33); cost_usd = total_tokens * 0.000001 is only stored on the in-memory job (2202)
**Fix:** Pass ctx.project_id (fall back to personal project), meter token quantity/cost, and log meter-write failures at error level
**Verifier:** Verified in code on fix/audit-tail. (1) main.rs:2203-2211 passes user_id as both user_id and project_id to record_job_usage, even though a correct project_id was resolved in the same function at main.rs:1643 (ctx_project_id from user.project_id() with DEFAULT_PROJECT_ID fallback) and used for the LLM call at :2020 — usage_meters.project_id has no FK (migration 20260226000004_audit_metering.sql), s

### 35. [medium/gap] `crates/knowledge-service/src/main.rs:1109`
**Claim:** POST /ingest/source (the OpenAlex/NTRS sweep) is fire-and-forget with no job record: it returns {status:'started'} with no id, and a sweep that fails immediately after spawn is visible only in server logs — the ingest-job status machinery exists but this path doesn't use it
**Evidence:** tokio::spawn(async move { … Err(e) => tracing::error!("source ingest failed: {e}") }) then Ok(Json(json!({"status":"started", …"note":"runs in background; monitor via /graph/stats and /embeddings/stats"}))) (main.rs:1079-1115) — RunReport.docs/failed counts are dropped after logging
**Fix:** Create an IngestJob entry (same map/DB as /ingest-job) for source sweeps and update it from the RunReport so clients can poll completion and failure
**Verifier:** CONFIRMED by reading the code on fix/audit-tail. (1) /ingest/source → ingest_source_run (crates/knowledge-service/src/main.rs:3448, handler at 1018) spawns tokio::spawn at main.rs:1079 and returns {"status":"started","source","max_docs","note"} at main.rs:1109-1114 — no job id. (2) The job machinery exists and is unused here: state.ingest_jobs (main.rs:59-60) is populated only by submit_ingest_job

### 36. [medium/silent-failure] `crates/knowledge-service/src/main.rs:1257`
**Claim:** POST /embed/bulk returns 'embedding started' and then swallows every failure into server logs — there is no job object, so a run where 100% of documents failed to embed/store is indistinguishable from success to the caller (only the global /embeddings/stats count hints at it)
**Evidence:** tokio::spawn { … Err(e) => { tracing::error!(error = %e, "batch embed failed"); failed += chunk.len(); } … tracing::info!(embedded, failed, "bulk embed complete"); } then response json!({"status":"embedding","documents":doc_count,…}) (main.rs:1210-1261) — embedded/failed never leave the process. Also stores each document as ONE embedding (chunk_idx: 0, no chunking), so long docs are silently truncated at the provider's token limit
**Fix:** Give bulk_embed a job id in the ingest_jobs map with embedded/failed counters; chunk documents through ontology::chunk before embedding
**Verifier:** CONFIRMED by reading /Users/siddharthakovid/Downloads/marc27-core (branch fix/audit-tail, HEAD 984cfce). (1) Fire-and-forget with no job object: crates/knowledge-service/src/main.rs:1210-1255 — bulk_embed tokio::spawn's the work; `embedded`/`failed` counters exist only inside the closure and are emitted solely via tracing (warn at 1237 "embed store failed", error at 1244 "batch embed failed", info

### 37. [medium/gap] `crates/knowledge-service/src/main.rs:1875`
**Claim:** There is no way to upload a raw document (PDF or otherwise) to the cloud for ingestion: knowledge-service exposes no upload endpoint (text must be inlined in JSON or already be in R2/at a URL), and PDF text extraction is unwired on every cloud path — honest error on R2Path, silent skip on Corpus, garbage on Url — despite documents being core to 'ingest from any node, anytime'
**Evidence:** Router (main.rs:3423-3478) has no upload route; R2 is read-only on ingest (r2_store.get / list_objects only); R2Path rejects %PDF with 'text extraction is not wired on the R2 ingest path' (1895-1905); Corpus sweep skips *.pdf without logging it to the job (1968-1972: if obj.key.ends_with(".pdf") … { continue; })
**Fix:** Add a multipart/bytes upload endpoint that writes to R2 under the tenant prefix and enqueues an ingest job; wire a PDF text extractor (marker/pdftotext sidecar) behind the R2Path/Url branches
**Verifier:** Verified real by reading code. knowledge-service router (crates/knowledge-service/src/main.rs:3425-3475) has no upload route and IngestSource (crates/core/src/ingest/job.rs:9-18) has no inline-document variant. R2Path honestly rejects %PDF (main.rs:1895-1905); Corpus sweep silently drops *.pdf with an unlogged `continue` (main.rs:1966-1973, job still succeeds if other text files exist); Url arm ha

### 38. [medium/silent-failure] `crates/core/src/identity/data_access.rs:207`
**Claim:** assert_corpus_access fails OPEN on any DB error: Err(_) from get_by_id (including connection failures, not just 'row not found') returns Ok(()), so the project-boundary write gate on /embed, /embed/bulk and /ingest/source silently grants access during a Postgres outage
**Evidence:** let corpus = match marc27_db::queries::corpora::get_by_id(pool, corpus_id).await { Ok(c) => c, // Not a platform corpus row — nothing project-bound to enforce. Err(_) => return Ok(()), };
**Fix:** Match DbError::NotFound => Ok(()) specifically and propagate all other errors
**Verifier:** CONFIRMED by reading the code. data_access.rs:204-208 (fix/audit-tail, current) matches the evidence verbatim: `Err(_) => return Ok(())` on get_by_id. DbError (crates/db/src/error.rs:10-22) distinguishes NotFound from Sqlx(sqlx::Error) — get_by_id (crates/db/src/queries/corpora.rs:41-47) maps missing rows to NotFound and propagates connection/pool/timeout errors as Sqlx — but the catch-all swallow

### 39. [medium/gap] `crates/knowledge-service/src/main.rs:389`
**Claim:** Corpus activation is a Postgres row with no data-plane effect: it returns 201 with storage_prefix 'corpora/{project}/{slug}/indexed/' that nothing in the codebase ever writes to or reads from, and the Corpus ingest job reads a different prefix ('corpora/{slug}/') with no project scoping
**Evidence:** corpora::activate is a bare INSERT…RETURNING (db/queries/corpora.rs:87-118); grep for storage_prefix shows only the format!() at main.rs:389, the INSERT, and the model field — no consumer; run_ingest_job Corpus branch uses format!("corpora/{slug}/") (main.rs:1936)
**Fix:** Either wire activation to trigger/point at real indexed data (and make Corpus ingest honor the activation prefix), or document activation as billing-entitlement-only
**Verifier:** CONFIRMED. activate_corpus (knowledge-service/src/main.rs:345-404) is membership-check + bare INSERT (db/queries/corpora.rs:96-108) returning 201 — no job, download, or indexing is triggered. storage_prefix 'corpora/{project}/{slug}/indexed/' (main.rs:389) has no consumer anywhere: only the format!(), the INSERT bind (corpora.rs:107), and the model field (models/corpus.rs:123). The activation life

### 40. [medium/silent-failure] `marc27-core/crates/knowledge-service/src/main.rs:2099`
**Claim:** IngestJobRequest.corpus_slug (and GraphQL corpusSlug input) is accepted and documented ('Corpus slug for provenance') but never read by run_ingest_job; embeddings are hardcoded to corpus_id = Uuid::nil(), so ingested data is never associated with the caller's requested corpus.
**Evidence:** run_ingest_job destructures req but never uses req.corpus_slug; the embed path sets `let corpus_id = Uuid::nil();` (main.rs:2099) and provenance is recorded with `project_id: None` (main.rs:2081). grep confirms corpus_slug is referenced only in the struct def (core/ingest/job.rs:47) and the unrelated activate_corpus struct. GraphQL submit_ingest_job forwards corpus_slug (api/routes/graphql/ingest.rs:79) into the same ignored field.
**Fix:** Resolve corpus_slug -> corpus_id (corpora::get_by_slug) in submit_ingest_job and thread it into IngestContext/StoreInput, or drop the field from the request/GraphQL input so callers aren't told it does something it doesn't.
**Verifier:** CONFIRMED, not refuted — verified by reading the full execution path on branch fix/audit-tail (HEAD 729d4d6). (1) The field is accepted and advertised: crates/core/src/ingest/job.rs:47 declares `pub corpus_slug: Option<String>` documented "Corpus to associate results with (for provenance)", and GraphQL submitIngestJob forwards `input.corpus_slug` into the POST {ks}/ingest-job body (crates/api/src/

### 41. [medium/silent-failure] `marc27-core/crates/core/src/ingest/job.rs:45`
**Claim:** IngestJobRequest.budget_max_usd (and GraphQL budgetMaxUsd, documented 'Max budget in USD') is accepted but never enforced anywhere; ingest jobs run unbounded regardless of the stated cap.
**Evidence:** grep for budget_max_usd across the workspace yields only the struct field (core/ingest/job.rs:45) and the GraphQL input (api/routes/graphql/ingest.rs:78,213). run_ingest_job computes cost_usd after the fact (main.rs:2202) but there is no check against any budget during or before extraction/embedding.
**Fix:** Enforce budget_max_usd inside run_ingest_job (abort the per-source loop once accumulated cost_usd exceeds it) or remove the field from the public request/GraphQL surface.
**Verifier:** CONFIRMED. IngestJobRequest.budget_max_usd (crates/core/src/ingest/job.rs:45) and GraphQL IngestJobInput.budget_max_usd (crates/api/src/routes/graphql/ingest.rs:213, documented "Max budget in USD.") are accepted and forwarded (ingest.rs:78) to the knowledge service, but never enforced. The sole consumer is knowledge-service submit_ingest_job (crates/knowledge-service/src/main.rs:1407) → run_ingest

### 42. [medium/gap] `marc27-core/crates/api/src/routes/knowledge_proxy.rs:30`
**Claim:** The knowledge-service source-sweep ingest endpoint /ingest/source (ntrs|openalex background paper ingestion) is advertised by the KS /capabilities doc but has no gateway proxy route, so it 404s through api.marc27.com and is only reachable on the Railway private network.
**Evidence:** KS registers `.route("/ingest/source", post(ingest_source_run))` (main.rs:3448) and tool_list advertises it as ingest_source at endpoint '/ingest/source' (main.rs:2336-2340), surfaced publicly via proxy_capabilities. But knowledge_proxy::routes() (knowledge_proxy.rs:30-61) proxies /ingest-job* and /graph/ingest but NOT /ingest/source; smart_404 ENDPOINTS also omits it. A caller hitting /api/v1/knowledge/ingest/source gets a 404.
**Fix:** Either add a proxy route for /ingest/source (authed, mirroring proxy_ingest_submit) or remove it from the KS capabilities advertisement so agents aren't pointed at an unreachable path.
**Verifier:** CONFIRMED. KS registers /ingest/source with a real handler (knowledge-service/src/main.rs:3448, handler at :1018 with tenant/RBAC/NTRS/OpenAlex logic) and advertises it via /capabilities (main.rs:2336-2340), which the gateway's proxy_capabilities (knowledge_proxy.rs:341-349) streams verbatim to public callers. But knowledge_proxy::routes() (knowledge_proxy.rs:30-61) has no /ingest/source route; ro

### 43. [medium/gap] `PRISM/crates/cli/src/main.rs:2309`
**Claim:** The cloud cannot command a PRISM node to ingest a local file: node-local ingest tools (ingest_file, ingest, ingest_watch) are approval-gated, and the platform->node relay ALWAYS calls invoke_tool with approve=false, so a relayed ingest is honestly refused. The 'platform commands a node to ingest' direction is unavailable.
**Evidence:** Relay executor: `chat.invoke_tool(&req.tool, req.args, Some(caller), false)` — approve hardcoded false (cli/main.rs:2309). ingest_file/ingest/ingest_watch have requires_approval: true (agent/command_tools.rs:257,248,266). service.rs:298 rejects gated tools when !approve with 'approval-gated ... remote relay callers cannot approve'. This is by-design security, but per the 'ingest from/command any node' goal it is a functional gap.
**Fix:** If platform-commanded node ingestion is a required capability, add an owner-preauthorized allowlist (persisted node-side consent) so specific ingest tools can run under relay without an interactive human; otherwise document that this direction is intentionally unsupported.
**Verifier:** Verified real. The only cloud→node command channel is PlatformMessage::InvokeTool (proto/src/lib.rs:203-247; no dedicated ingest message), and its relay executor hardcodes approve=false (cli/main.rs:2372, comment at 2369: "approve=false ALWAYS"). All three node-local ingest tools are approval-gated (command_tools.rs:248 ingest, 257 ingest_file, 266 ingest_watch), and service.rs:294-307 honestly re

### 44. [medium/fake] `/Users/siddharthakovid/Downloads/marc27-core/crates/core/src/ingest/job.rs:31`
**Claim:** ExtractionMode::Raw ('Just store raw text, no extraction') is advertised in the API but unimplemented — Raw mode still runs LLM extraction and graph writes and never stores raw text; mode only gates the embedding step, so Graph/Embed/Raw all behave almost identically.
**Evidence:** job.rs:30-31 declares `Raw, /// Just store raw text, no extraction`. In knowledge-service main.rs the only uses of req.mode are line 1421 (stored on the job record) and line 2094 `matches!(req.mode, Full | Embed)` gating embeddings. extract_entities is called unconditionally (main.rs:2017) and graph ingest is not mode-gated (main.rs:2047). No code path stores raw text for Raw mode.
**Fix:** Either implement Raw (store text to R2/chunks without LLM extraction) and gate extraction/graph on mode, or delete the Raw variant and reject it at deserialization.
**Verifier:** CONFIRMED. ExtractionMode::Raw (job.rs:31, "Just store raw text, no extraction") is user-reachable — the GraphQL API advertises "raw" as a valid mode (crates/api/src/routes/graphql/ingest.rs:207) and forwards it to the knowledge service — but is never implemented. Repo-wide grep shows zero match arms on ExtractionMode::Raw; the only mode gate in the workspace is knowledge-service/src/main.rs:2093-

### 45. [medium/gap] `/Users/siddharthakovid/Downloads/PRISM/crates/cli/src/main.rs:4540`
**Claim:** Marketplace OCR engines (PaddleOCR-VL, glm-ocr etc., seeded 2026-07-06 with 'users pick their OWN OCR engine') are catalog-only for ingestion — every ingest path hardcodes pymupdf, and the runtime's glm-ocr/paddleocr-vl handlers are unreachable from any ingest entry point, so scanned PDFs can only get the 'PDF may be scanned. Use glm-ocr or paddleocr-vl' warning with no way to act on it.
**Evidence:** The only two callers of runtime /run for PDF extraction hardcode the engine: PRISM cli/src/main.rs:4540 `"model": "pymupdf"` and marc27-core core/src/research/engine.rs:3292 `"model": "pymupdf"`. marc27-core/runtime/main.py:102-115 registers glm-ocr and paddleocr-vl and main.py:627 warns scanned PDFs to use them, but no ingest surface (CLI flag, agent tool arg, ingest-job field) lets a caller select an OCR engine; db/migrations/20260706000001_seed_ocr_embedding_models.sql seeds them as marketplace entries only.
**Fix:** Add an `--ocr-engine` / `ocr_model` parameter threaded from `prism ingest` (and IngestJobRequest) through to the runtime /run call, defaulting to pymupdf, and auto-retry with the configured OCR engine when pymupdf returns the scanned-PDF warning.
**Verifier:** Defect is REAL — verified end-to-end in both repos. (1) Hardcoded engine, both callers confirmed: PRISM /Users/siddharthakovid/Downloads/PRISM/crates/cli/src/main.rs:4540 `extract_pdf_text_with_runtime` builds `"model": "pymupdf"` with no engine parameter (fn signature takes only runtime_url+path); all ingest paths route through it (extract_platform_ingest_text :4579, run_platform_ingest_file :471

### 46. [medium/gap] `/Users/siddharthakovid/Downloads/PRISM/crates/server/src/handlers/data.rs:56`
**Claim:** PRISM node HTTP POST /api/data/ingest is an honest 501 — node-HTTP document ingestion (core to 'ingest from any node, anytime') is unwired, and the error text directs callers to run a `prism ingest` CLI command (against the no-exit-to-CLI rule); the only working node-HTTP route is the indirect /api/tools/ingest_file/run.
**Evidence:** data.rs:56-68 returns StatusCode::NOT_IMPLEMENTED with message 'Ingestion is not available on this endpoint... Use `prism ingest <file>` or the knowledge-service ingest API.' The route is still registered at server/src/router.rs:104.
**Fix:** Wire the handler to the same IngestPipeline/command-tool execution the /api/tools path uses (accept file path or inline text + corpus), or at minimum point the 501 at POST /api/tools/ingest_file/run which works today.
**Verifier:** Verified on main. data.rs:56-68 unconditionally returns 501 NOT_IMPLEMENTED with a message directing callers to the `prism ingest` CLI (no-exit-to-CLI violation in the error text); the handler ignores NodeState entirely — no pipeline behind it. Route is registered and auth-gated at router.rs:103-104 (merged :192). Full route scan confirms no other node-HTTP ingest endpoint (the "knowledge-service 

### 47. [medium/gap] `/Users/siddharthakovid/Downloads/marc27-core/crates/knowledge-service/src/main.rs:1427`
**Claim:** Ingest jobs live only in an in-process HashMap — any knowledge-service restart/redeploy silently erases all job status, so `prism ingest --status` and GET /ingest-job/{id} 404 on jobs that were accepted moments earlier, with no way to tell 'never existed' from 'lost'.
**Evidence:** main.rs:1427 `state.ingest_jobs.write().await.insert(job_id, job)` into `Arc<RwLock<HashMap<Uuid, IngestJob>>>`; get_ingest_job (main.rs:2244-2263) reads only this map and returns 404 otherwise; no DB persistence of job rows anywhere in the file.
**Fix:** Persist job rows (id, tenant, status, counts, log tail) to Postgres on create/update; serve status reads from the table.
**Verifier:** Verified real. Ingest jobs exist only in an in-process HashMap: KnowledgeState.ingest_jobs (main.rs:59-61) is created empty at startup (main.rs:3269-3271) with no rehydration; submit_ingest_job inserts only into the map (main.rs:1427); all status updates in run_ingest_job are in-memory get_mut writes (e.g. main.rs:1650-1655); get_ingest_job (main.rs:2244-2262) and list_ingest_jobs (main.rs:2266-22

### 48. [low/gap] `crates/cli/src/main.rs:4382`
**Claim:** Claimed format support for json/jsonl/owl/cif is raw-text ingestion only: files are read with read_to_string, chunked at 48K chars, and submitted as free-text 'query' sources — no JSON/OWL/CIF structure-aware parsing anywhere, so an OWL ontology or CIF crystal structure is ingested as text soup.
**Evidence:** ingest_backend maps 'pdf|json|jsonl|owl|cif|txt|md' to PlatformText (main.rs:4380-4386); extract_platform_ingest_text non-PDF branch is just `std::fs::read_to_string(path)` (main.rs:4534-4537); submit body is `"source": { "type": "query", "query": chunk }` (main.rs:4553-4555).
**Fix:** Either add structure-aware handling (CIF -> entities, OWL -> ontology import, JSON -> records) or narrow the advertised format list / document that these are treated as plain text.
**Verifier:** CONFIRMED, and worse than claimed. PRISM CLI: crates/cli/src/main.rs:4443-4448 maps json/jsonl/owl/cif (plus pdf/txt/md) to PlatformText; the non-PDF branch of extract_platform_ingest_text (main.rs:4597-4601) is bare std::fs::read_to_string; chunks are cut at 48K chars (main.rs:4490-4531) and submitted as {"source":{"type":"query","query":chunk}} (main.rs:4616-4619). No JSON/OWL/CIF structure-awar

### 49. [low/fake] `crates/ingest/src/graph.rs:296`
**Claim:** nodes_created/edges_created report the number of MERGE statements executed, not actual creations — re-ingesting the same file prints 'N nodes, M edges written to Neo4j' again even though every MERGE matched an existing node and created nothing.
**Evidence:** graph.rs:276-292 increments `nodes_created += count` where count = statements.len() per batch regardless of MERGE match-vs-create; CLI renders this as 'Graph: {nodes} nodes, {edges} edges written to Neo4j' (main.rs:4756).
**Fix:** Rename to nodes_upserted/edges_upserted in output text, or read the Neo4j tx result counters (stats containsUpdates/nodes_created) for true creation counts.
**Verifier:** Verified real. graph.rs:276-292 increments nodes_created/edges_created by statements.len() per batch; the Neo4j HTTP request never sets includeStats and CypherResponse (graph.rs:55-59) parses only results/errors, while the MERGE queries return only n.name/type(r) — so match-vs-create is structurally indistinguishable and the counts are statement counts, not creations. No caller corrects it: pipeli

### 50. [low/silent-failure] `crates/ingest/src/pipeline.rs:171`
**Claim:** Pipeline error logs print only the outermost anyhow context ('LLM request to <url> failed'), hiding the root cause — a 120s timeout and a connection refused produce the same message, which cost real diagnosis time in this audit's live runs.
**Evidence:** pipeline.rs:171 `tracing::error!("LLM extraction failed: {e}")` uses Display (top context only); llm/src/lib.rs:814-816 `.with_context(|| format!("LLM request to {url} failed"))?` wraps the reqwest error whose Display carries the timeout/refused detail.
**Fix:** Log with the alternate formatter `{e:#}` (or {:?}) to include the anyhow cause chain.
**Verifier:** Confirmed by reading the live code. pipeline.rs:171 logs the anyhow::Error with plain `{e}` (Display), which prints only the outermost context; no `{:#}`/`{:?}` chain formatting exists anywhere in pipeline.rs. The transport error path is exactly as claimed: extract_entities_with_mapping (ontology.rs:240) → generate → LlmClient::generate_json (llm/src/lib.rs:364) → post() (lib.rs:805), where lib.rs

### 51. [low/silent-failure] `crates/cli/src/main.rs:4977`
**Claim:** --corpus is silently ignored for local tabular files: `prism ingest data.csv --corpus my-corpus` runs the local pipeline which never receives the corpus, with no warning (unlike --mapping, which does warn on the platform path).
**Evidence:** handle_ingest routes LocalTabular to run_local_ingest_file (main.rs:4977-4991) whose signature (main.rs:4580-4592) has no corpus parameter; corpus is only passed to run_platform_ingest_file. The help text (main.rs:179) says 'Corpus slug to associate with the ingested data' unconditionally.
**Fix:** Print a warning mirroring the --mapping one when --corpus is set on a local tabular ingest, or thread corpus into the local pipeline as a node/label property.
**Verifier:** Defect confirmed by reading the live code. `prism ingest data.csv --corpus X` dispatches (main.rs:2749) to handle_ingest (main.rs:5015), which routes LocalTabular files (csv/tsv/parquet/pq, main.rs:4444) to run_local_ingest_file (main.rs:5041-5054) WITHOUT passing corpus; run_local_ingest_file's signature (main.rs:4643-4655) has no corpus parameter and the prism_ingest crate contains zero referenc

### 52. [low/gap] `crates/agent/src/command_tools.rs:606`
**Claim:** knowledge_ingest's tool description and the CLI help promise that a free-text `query`'s text is extracted into the graph, but the server (post-fix) instead web-searches academic sources and ingests up to 10 matching paper title+abstracts — the agent will mispredict what lands in the graph.
**Evidence:** command_tools.rs:606 'Submit a background extraction job into the MARC27 knowledge graph from a `url` or free-text `query`. Entity extraction runs asynchronously server-side'; cli/main.rs (KnowledgeCommands::Ingest) help: 'Free-text query to extract entities/embeddings from.' Actual server behavior: knowledge-service/src/main.rs:1822-1875 searches search_all and ingests query_papers_to_texts (≤ QUERY_INGEST_MAX_PAPERS = 10 abstracts), never the query text.
**Fix:** Reword both descriptions to 'searches academic sources (arXiv/PubMed/Semantic Scholar/OpenAlex) for the query and ingests the top matching papers' abstracts'.
**Verifier:** Verified real. PRISM main still promises query-text extraction in three places: command_tools.rs:606 (tool description "background extraction job ... from a `url` or free-text `query`"), command_tools.rs:1289 (schema: "Free-text to extract entities/embeddings from."), cli/main.rs:6321 (same help text). The server (marc27-core crates/knowledge-service/src/main.rs:1821-1875, branch fix/audit-tail) i

### 53. [low/silent-failure] `PRISM/app/plugins/bootstrap.py:127`
**Claim:** Registration of whole ingestion-relevant tool groups (web, agent_runs, builtin skills incl. acquire_materials, plugins) is wrapped in bare `except Exception: pass` — an import/registration failure silently removes the capability from the agent's catalog with zero log
**Evidence:** lines 122-128 (`from app.tools.web import create_web_tools ... except Exception:\n        pass`), 148-153 (agent_runs), 202-208 (load_builtin_skills), 250-252 (discover_all_plugins) — unlike the memory block (line 119-120) which at least logs a warning
**Fix:** Replace bare `pass` with logger.warning(..., exc_info=True) naming the lost tool group
**Verifier:** Confirmed by reading the code. All four cited blocks in PRISM/app/plugins/bootstrap.py are bare `except Exception: pass` with zero logging: web tools (123-128), agent_runs (148-153), builtin skills incl. acquire_materials (203-208; verified ACQUIRE_SKILL name="acquire_materials" in app/tools/skills/acquisition.py:110-111, a core ingestion skill — one bad import kills all 7 builtin skills), and plu

### 54. [low/silent-failure] `PRISM/app/mcp_client.py:58`
**Claim:** Configured external MCP servers (user-added data/ingestion sources) that fail discovery are skipped with a literal 'skip silently' — their tools vanish with no log or user-visible notice
**Evidence:** `except Exception:\n        pass  # Server unavailable, skip silently` (lines 58-59), and the per-server 45s timeout wrapper also collapses to `tools = []` (lines 108-109)
**Fix:** Log a warning per failed server and return an `unavailable_servers` list from discover_and_register_mcp_tools for surfacing
**Verifier:** Confirmed. app/mcp_client.py:58-59 swallows all per-server discovery errors with `except Exception: pass # Server unavailable, skip silently`, and the 45s timeout wrapper at lines 104-110 collapses to `tools = []` under another bare except — no logging module is even imported. The sole production caller (app/plugins/bootstrap.py:266-272) adds a third `except Exception: pass` and discards the retur

### 55. [low/silent-failure] `PRISM/app/plugins/loader.py:61`
**Claim:** Broken plugin files in ~/.prism/plugins/ and failing entry-point plugins are silently skipped — a user's custom collector/ingestion plugin can fail to load with no signal anywhere
**Evidence:** discover_entry_point_plugins lines 32-35 and discover_local_plugins lines 61-62 both end `except Exception:\n            pass` with no logging
**Fix:** logger.warning with the plugin name and exception in both loops
**Verifier:** Verified real by reading code. PRISM/app/plugins/loader.py has bare `except Exception: pass` at lines 32-33 and 34-35 (entry-point plugins) and 61-62 (local ~/.prism/plugins/*.py files); the file imports no logging module. The sole production caller, app/plugins/bootstrap.py:235-252, wraps discover_all_plugins() in yet another bare `except Exception: pass` (251-252) — so even a total plugin-subsys

### 56. [low/gap] `PRISM/app/tools/skills/reporting.py:296`
**Claim:** A report requested as PDF silently downgrades to markdown when weasyprint/markdown aren't installed — the result carries format='markdown' but never says the requested PDF conversion was skipped
**Evidence:** `except ImportError:\n                pass  # PDF deps not installed — Markdown still saved` (lines 296-297); result dict gains no note about the downgrade
**Fix:** Add `result["pdf_skipped"] = "weasyprint/markdown not installed — pip install 'prism-platform[reports]'"` in the except branch
**Verifier:** CONFIRMED by reading the real execution path. In /Users/siddharthakovid/Downloads/PRISM/app/tools/skills/reporting.py, `_generate_report` advertises "pdf" in its input schema enum (line 338) and "Optionally converts to PDF" in the skill description (line 307). When format="pdf" is requested (via kwarg line 44 or prefs.report_format fallback lines 51-52), execution takes the else branch at line 281

### 57. [low/silent-failure] `crates/knowledge-service/src/main.rs:1044`
**Claim:** POST /ingest/source with an unknown source returns 500 internal_error whose machine hint says 'Transient server-side failure - safe to retry once with backoff' — a client-input error mislabeled as retryable, so agents retry a request that can never succeed
**Evidence:** other => return Err(KsError(CoreError::Internal(format!("unknown source '{other}' (supported: ntrs, openalex)")))) (main.rs:1044-1048) mapped by KsError::into_response to (500, "internal_error") with the retry hint (main.rs:268, 283)
**Fix:** Use CoreError::Validation so it maps to 422 validation_error with the 'message names the problem' hint
**Verifier:** CONFIRMED. At /Users/siddharthakovid/Downloads/marc27-core/crates/knowledge-service/src/main.rs:1044-1048, an unknown `source` in POST /ingest/source (route registered directly at main.rs:3448, no re-mapping proxy) returns Err(KsError(CoreError::Internal("unknown source '{other}' (supported: ntrs, openalex)"))). KsError::into_response maps CoreError::Internal(_) to (500, "internal_error") at main.

### 58. [low/fake] `crates/core/src/ingest/extractor.rs:157`
**Claim:** tokens_used is a fabricated estimate (response length / 4, output-only — input tokens ignored), and this number flows into the user-visible job cost_usd, so the 'Done: … ~$X USD' figure shown to clients is invented, not measured
**Evidence:** tokens_used: full_text.len() as u64 / 4 (extractor.rs:157); main.rs:2202 let cost_usd = total_tokens as f64 * 0.000001; // rough: $1/M tokens — displayed in job.log and job.cost_usd
**Fix:** Have the LLM Service return real usage (prompt+completion tokens) in its SSE tail and propagate it; until then label the field estimated_cost_usd
**Verifier:** CONFIRMED, with one correction: the cited files are in marc27-core, not PRISM (PRISM has no tokens_used at all). extractor.rs:157 fabricates tokens_used as output-chars/4 — full_text is the LLM completion only, so input tokens (up to 60K chars of doc + system prompt) are ignored and no real usage is ever parsed (llm_client.rs has no usage/prompt_tokens/completion_tokens handling). knowledge-servic

### 59. [low/silent-failure] `crates/knowledge-service/src/main.rs:1968`
**Claim:** Corpus ingest silently drops every .pdf/.bin/.npy object without a job-log mention — a mixed corpus reports Completed having ingested only the text files, with no indication that most of the corpus was skipped
**Evidence:** for obj in &listing.objects { if obj.key.ends_with(".pdf") || obj.key.ends_with(".bin") || obj.key.ends_with(".npy") { continue; } … } — the job logs 'Found N objects' (1958) but never how many were skipped; only the all-skipped case fails honestly (1988)
**Fix:** Count skipped objects and job.log 'skipped M binary objects (no PDF extractor wired)' so the Completed report is honest
**Verifier:** CONFIRMED on fix/audit-tail (no fix landed; latest commits 984cfce/#70 touched other paths). In /Users/siddharthakovid/Downloads/marc27-core/crates/knowledge-service/src/main.rs, the Corpus branch of run_ingest_job: (1) line 1958 logs only "Found {N} objects in corpus"; (2) lines 1966-1973 drop every .pdf/.bin/.npy object with a bare `continue` — no counter, no job.log, nothing user-visible; (3) l

### 60. [low/broken] `marc27-core/crates/knowledge-service/src/main.rs:2203`
**Claim:** Ingest-job usage is metered with the user's UUID passed in the project_id position, so knowledge_ingest usage/billing is misattributed to a nonexistent project equal to the user id.
**Evidence:** `record_job_usage(&db, job_id, "knowledge_ingest", user_id, user_id, None)` (main.rs:2203-2211); the 5th arg is `project_id: Uuid` (core/metering/usage.rs signature) and the inline comment even says 'project_id fallback'. The real project id is available in IngestContext (ctx.project_id, main.rs:1443) but is not used for metering.
**Fix:** Pass ctx.project_id (falling back to the DEFAULT_PROJECT_ID uuid already computed at main.rs:1643) into record_job_usage instead of user_id.
**Verifier:** Confirmed by reading the code. main.rs:2202-2211 calls record_job_usage(&db, job_id, "knowledge_ingest", user_id, user_id, None); the 5th parameter of record_job_usage (crates/core/src/metering/usage.rs:12-19) is project_id: Uuid, which is inserted into usage_meters.project_id. The real project id is available in scope (IngestContext.project_id from main.rs:1443, destructured as ctx_project_id at 

### 61. [low/gap] `marc27-core/crates/knowledge-service/src/main.rs:1427`
**Claim:** Ingest job state (status, progress, logs, counters) lives only in an in-process RwLock<HashMap>, so it is lost on knowledge-service restart and is not shared across replicas; a status poll (/ingest-job/{id}) returns 404 after a restart or when it lands on a different replica, even though the ingested data itself is durably written.
**Evidence:** `state.ingest_jobs.write().await.insert(job_id, job)` (main.rs:1427); get_ingest_job/list_ingest_jobs read from the same in-memory map (main.rs:2250,2271). There is no DB persistence of ingest_jobs. The actual writes (FalkorDB graph/vectors, Postgres provenance) are durable, so this is a job-tracking durability gap, not data loss.
**Fix:** Persist ingest job rows to Postgres (or Redis) keyed by job_id+tenant so status survives restarts and is replica-shared.
**Verifier:** Confirmed. Ingest job state exists only in KnowledgeState.ingest_jobs, an Arc<RwLock<HashMap<Uuid, IngestJob>>> (crates/knowledge-service/src/main.rs:59-61) initialized empty at startup (main.rs:3269). submit_ingest_job inserts into it (main.rs:1427), the background task mutates only it, and get_ingest_job/list_ingest_jobs (main.rs:2250-2261, 2271-2276) read only it — so any restart or different r

### 62. [low/silent-failure] `/Users/siddharthakovid/Downloads/marc27-core/crates/knowledge-service/src/main.rs:1967`
**Claim:** Corpus-slug ingestion silently skips every .pdf/.bin/.npy object in the corpus without counting or reporting the skips, so a mostly-PDF corpus 'completes' having ingested only its stray text files.
**Evidence:** main.rs:1967-1971: `// Skip non-text files for now` then `if obj.key.ends_with(".pdf") || ... { continue; }` — no skip counter, no job.log mention; the job only fails if texts ends up empty (main.rs:1996).
**Fix:** Count skipped objects and write 'skipped N binary objects (PDF extraction not wired on this path)' into the job log, mirroring the honest R2Path PDF refusal at main.rs:1893.
**Verifier:** Verified real on fix/audit-tail. crates/knowledge-service/src/main.rs:1966-1973 silently `continue`-skips every .pdf/.bin/.npy corpus object with no counter and no job.log entry; per-object R2 fetch failures (1981-1983) are also tracing::warn-only. The job fails only if texts is empty (1987-1990) and otherwise completes with "Done: N entities, M rels" (2213-2224) — no skip report. The audit branch

### 63. [low/silent-failure] `/Users/siddharthakovid/Downloads/marc27-core/crates/knowledge-service/src/main.rs:2065`
**Claim:** Per-entity provenance writes during ingest jobs are fire-and-forget — `let _ =` discards Postgres errors, so provenance (the audit backbone) can be silently missing for entities that did land in the graph; same pattern for the research engine's R2 paper archive.
**Evidence:** knowledge-service main.rs:2065 `let _ = marc27_db::queries::provenance::record(...)`; core/src/research/engine.rs:3348 `let _ = crate::marketplace::storage::ObjectStore::put(...)` for the 'permanent archive'.
**Fix:** At minimum tracing::warn! with job_id/entity on failure and count provenance failures in the job log; consider failing the job if provenance is contractually required for graph writes.
**Verifier:** Confirmed at both cited sites. (1) crates/knowledge-service/src/main.rs:2065-2088: per-entity provenance in the ingest-job loop is `let _ = marc27_db::queries::provenance::record(...)`; record() (crates/db/src/queries/provenance.rs:27-53) returns Result with no internal logging, so Postgres failures are fully silent — no tracing::warn, no job.log entry, no counter — while the entity has already la

## What was verified WORKING (keep true)
### PRISM local ingestion engine (crates/ingest + CLI ingest verbs)
- CSV/TSV parsing: prism ingest file.csv -> CsvConnector::load (polars, crates/ingest/src/connectors/csv.rs:13) -> SchemaDetector::detect (schema.rs:42) -> validation::validate (validation.rs:34) — LIVE-VERIFIED (2 rows / 3 columns detected correctly)
- Parquet parsing: ParquetConnector::load (connectors/parquet.rs:13) -> same pipeline — real polars ParquetReader, honest error on open/read failure
- Neo4j graph storage: Neo4jGraphStore::upsert (graph.rs:270-300) — real HTTP tx/commit Cypher MERGE in batches of 50, bails honestly on connect failure, non-2xx, and Cypher errors (graph.rs:105-118); nothing is faked
- Qdrant vector storage: QdrantVectorStore::upsert (embeddings.rs:219-271) — real REST PUT /points?wait=true with auto collection creation (ensure_collection embeddings.rs:178), honest bail on non-2xx
- LLM client: prism-llm post() (crates/llm/src/lib.rs:806-836) — real HTTP with 429 backoff retries, honest bail on non-2xx and send failure; embeddings via real /v1/embeddings (lib.rs:389-412)
- Watch mode really watches: handle_ingest_watch (crates/cli/src/main.rs:5033-5154) — real 5s mtime-polling loop, initial scan + re-ingest of new/modified files, per-file errors printed and loop continues
- Platform PDF path: extract_pdf_text_with_runtime (main.rs:4470-4508) really POSTs the PDF to the local runtime /run (pymupdf) and bails honestly if runtime is down or returns no text; chunks really submitted to POST /knowledge/ingest-job with honest bail on non-2xx (main.rs:4543-4577)
- prism ingest --status: fetch_ingest_status (main.rs:4826-4902) — real platform GETs (graph/stats, embeddings/stats, ingest-jobs) with error_for_status, no canned data
- Server POST /api/data/ingest now returns an honest 501 not_implemented instead of the old fake 'accepted' (crates/server/src/handlers/data.rs:56-68) — desired behavior for an unwired path

### PRISM node HTTP + agent-tool ingestion surface (POST /api/data/ingest, /api/tools/{name}/run, ingest/ingest_file/ingest_watch/knowledge_ingest command tools, Python tool server ingestion tools, workflow tool steps, platform relay)
- HTTP POST /api/tools/ingest_file/run with {"approve":true} (PRISM/crates/server/src/router.rs:111 ExecuteTools-gated -> handlers/tools.rs:135 run_tool -> agent/src/service.rs:274 invoke_tool -> command_tools.rs:2716 execute_cli_command spawns own binary `prism ingest <path>`): for CSV/TSV/Parquet runs the real local pipeline crates/ingest/src/pipeline.rs:96-240 (polars load -> schema detect -> validation -> LLM entity extraction -> Neo4j upsert -> Qdrant upsert) — real storage when backends are up (but see soft-fail finding)
- Agent chat tool ingest/ingest_file (command_tools.rs:242,251, requires_approval=true): same CLI executor; headless chat denies un-approved gated tools honestly with an approval_required event, never silently auto-approves (service.rs:530-551,646-652)
- knowledge_ingest url mode (command_tools.rs:602 -> `prism knowledge ingest --url` cli/main.rs:6407ff -> POST {api}/knowledge/ingest-job IngestSource::Url): marc27-core knowledge-service/src/main.rs:1726-1820 really fetches (SSRF-revalidated redirects), strips HTML, extracts entities, writes graph/embeddings — real end-to-end; also invocable via relay/workflows since requires_approval=false
- Python tool server knowledge_write (app/tools/knowledge_write.py, requires_approval=True): embed/embed_bulk/graph_seed/graph_ingest/research_web_search are real POSTs to knowledge-service write endpoints; _platform_client returns honest {error} dicts (offline, not-logged-in, HTTP >=400), no fake success
- Remote relay PlatformMessage::InvokeTool (node/src/daemon.rs:902-990 -> cli/main.rs:2372 invoke_tool(..., approve=false)): approval-gated ingest tools refused with an explicit error; node without executor replies ok=false honestly; every invocation (incl. refusals) audited to ~/.prism/provenance.db (service.rs:348-383)
- POST /api/data/ingest (server/src/handlers/data.rs:56-68): honest 501 not_implemented — verified nothing behind it; the 501 is still accurate
- prism ingest --status / ingest tool --status (cli/main.rs:4890-5012): real authenticated GETs to /knowledge/graph/stats, /knowledge/embeddings/stats, /knowledge/ingest-jobs — no canned data
- PDF text extraction for platform ingest (cli/main.rs:4533-4604): real POST to local runtime /run model=pymupdf with honest bail on HTTP failure or empty text
- Python import_dataset (app/tools/data.py:214-250): real pandas read -> parquet write via DataStore.save (data_collectors/store.py:14-20) — local dataset store, honest errors
- Auth chokepoint resolve_agent_auth (cli/main.rs:5378): honest bail in offline mode and when not logged in — platform ingest paths cannot silently no-op
- Workflow tool steps (workflows/src/lib.rs:781-885): call the real node endpoint with minted loopback token; 4xx bails the step honestly; _node_token stripped from returned context

### PRISM Python-side ingestion tools (app/tools/, data_collectors/, memory subsystem, tool/MCP servers) + the PDF entry path they hand off to
- dataset(action='import') → app/tools/dataset.py:50 → app/tools/data.py:_import_dataset (real pandas read_csv/read_json/read_parquet) → data_collectors/store.py:14 DataStore.save → real Parquet + .meta.json on disk; honest errors for missing file/unsupported format
- acquire_materials skill → app/tools/skills/acquisition.py:7 → per-source collector.collect → normalizer.normalize_records (real dedup on source_id) → DataStore.save + optional CSV; missing-credential sources honestly surfaced in `skipped` and hard error when zero records
- knowledge_write (embed/embed_bulk/graph_seed/graph_ingest/research_web_search) → app/tools/knowledge_write.py → _platform_client.PlatformClient (never fakes: offline/unauthenticated/HTTP>=400/non-JSON all return {'error':...}) → all five routes verified live in marc27-core (crates/api/src/routes/knowledge_proxy.rs:41-52, crates/knowledge-service/src/main.rs:3442-3463)
- prior_art_search papers leg → app/tools/search.py:33 → literature_collector.collect_with_status → real arXiv Atom API + Semantic Scholar REST, per-source ok/error status honestly returned in `source_status`
- structure_import (CIF ingestion) → app/tools/structure_io.py:67 → real ase CIF parse/round-trip → MACE CacheStore.write_structure_cif + meta; honestly notes when pyiron StructureStore is unreachable instead of pretending
- Artifact memory machinery itself → app/tools/memory/store.py: real SQLite WAL + FTS5 triggers + BM25 + true cosine + RRF fusion; recorder.py does real atomic inserts + background embedding backfill (failures logged, artifact stays with NULL embedding and is skipped by the vector leg, BM25 still finds it)
- ingest_file on PDF: NO PRISM Python runs — it is the Rust command-tool `prism ingest` (crates/cli/src/main.rs:4510): base64 PDF POSTed to marc27-core runtime /run model=pymupdf which does REAL PyMuPDF (fitz) text+table extraction (marc27-core/runtime/main.py:547), then one real POST /knowledge/ingest-job per ≤48K-char chunk (route exists, knowledge-service main.rs:3455); honestly bails if runtime is down, response has no text, or job submit fails; CSV/parquet go through the real polars pipeline in crates/ingest
- web(action='read'/'search') → app/tools/web.py: real Firecrawl REST (availability probe checks the actual /scrape route, not just the port) → real DDG/BeautifulSoup fallbacks; each result labels its true source
- Tool.execute uniform error contract → app/tools/base.py:36-77: exceptions become {'error':...} dicts (never fake success), and memory-recorder failures can never alter or drop a tool result; tool_server.py/sidecar_server.py/mcp_server.py all route through it

### marc27-core cloud ingestion (knowledge-service, crates/knowledge-service/src/main.rs + core ingest/ontology/embeddings/graph modules)
- POST /embed → auth → derive_tenant → assert_corpus_access → EmbeddingClient.embed → FalkorDB :Chunk MERGE via embeddings::search::store_embedding (main.rs:703-754 → search.rs:93-131) — real vector write, real vecf32 index
- POST /graph/ingest → tenant override from auth → graph::ingest::batch_ingest → parameterized Entity/RELATED_TO MERGE in FalkorDB (main.rs:641-668 → graph/ingest.rs:57-142) — real graph writes, fails loudly on error
- POST /ingest/source with source=openalex → pipeline::ingest_source → live OpenAlex /works API (1-based paging correct, kill-switch guarded) → chunk_document (real chunker, chunk.rs:27-59) → embed_batch → store_embedding per chunk → LLM extract → PROV-O record_extraction (MERGE on deterministic assertion ids) + :IngestFailure records for failed chunks (pipeline.rs:77-282, openalex.rs:111-150, prov.rs:237-248, heal.rs:81-111) — real end-to-end
- POST /ingest-job with source=Url (HTML/text) → SSRF-validated fetch with per-hop redirect re-validation → HTML strip → extract_entities (real POST to LLM Service, SSE-parsed) → batch_ingest graph write + Postgres data_provenance rows + char-safe chunked embeddings into FalkorDB (main.rs:1725-2199, extractor.rs:47-159) — real when text/HTML
- POST /ingest-job with source=Query → research::web::search_all fans out to 4 real academic APIs (Semantic Scholar/arXiv/PubMed/OpenAlex, web.rs:387+) → title+abstract ingestion; zero-paper case fails the job honestly with per-source failure detail, never ingests the query string (main.rs:1822-1874)
- POST /ingest-job with source=R2Path (text object) → real R2Store.get → extract+embed; binary %PDF rejected with an honest Failed status pointing at the working path (main.rs:1875-1924)
- GET /ingest-job/{id} and /ingest-jobs → tenant-scoped reads of the real in-process job state machine (Pending→Fetching→Extracting→Completed/Failed with logs, progress, counts) (main.rs:2244-2277)
- Hotfix agent (heal_pass) + gap recording/auto-sweep (GAP_INGEST_AUTO=1) — real background graph work with real :IngestFailure / :KnowledgeGap nodes (main.rs:3284-3340, 885-974)
- POST /graph/seed (admin-gated) → real CSV fetch → background seed with errors logged; POST /embed/bulk → real batched embed+store writes (failures counted)

### marc27-core API ingestion routes + PRISM node relay path (who-can-ingest-from-where matrix)
- /api/v1/knowledge/graph/ingest: gateway proxy_graph_ingest (knowledge_proxy.rs:252, AuthUser) -> KS graph_ingest (main.rs:641) derives tenant from auth, overrides body tenant -> batch_ingest -> FalkorDB MERGE (core/graph/ingest.rs:60). REAL, tenant-safe.
- /api/v1/knowledge/embed: gateway proxy_embed (knowledge_proxy.rs:202) -> KS embed_text (main.rs:703) asserts corpus_access -> EmbeddingClient.embed -> store_embedding into FalkorDB vector store (main.rs:734). REAL.
- /api/v1/knowledge/embed/bulk: gateway proxy_embed_bulk -> KS bulk_embed (main.rs:1174) spawns background embed_batch -> store_embedding loop. REAL (async, honest 'started' response).
- /api/v1/knowledge/ingest-job with source type=url: gateway proxy_ingest_submit (knowledge_proxy.rs:277) -> KS submit_ingest_job (main.rs:1404) -> run_ingest_job (main.rs:1623) SSRF-validates URL, fetches, strips HTML, LLM-extracts entities, writes graph+embeddings+provenance. REAL.
- /api/v1/knowledge/ingest-job with source type=r2_path/corpus: run_ingest_job reads R2 objects, extracts text, writes graph+embeddings; binary PDF honestly rejected (main.rs:1895). REAL.
- KS /ingest/source (ntrs|openalex paper sweep): ingest_source_run (main.rs:1018) -> pipeline::ingest_source chunk->embed->extract->PROV-O. REAL but only reachable internally (see gap).
- Platform->node tool relay: invoke_node_tool (nodes.rs:142, visibility-gated) -> NodeRegistry.invoke_tool sends PlatformMessage::InvokeTool over WS, awaits real ToolInvokeResult with timeout, never fabricates (registry.rs:260) -> node daemon InvokeTool handler (daemon.rs:902) -> ChatService.invoke_tool (service.rs:274). REAL end-to-end, honest errors.
- Node WS registration + job/deployment result ingestion: handle_node_connection (nodes.rs:681) persists node identity via durable DB upsert, records job completion/provenance with node_owns_job guards. REAL.
- PRISM node->cloud push for tabular files (CSV/TSV/parquet): prism ingest -> LocalTabular backend (cli/main.rs:4444) -> Neo4j/Qdrant on the node itself (run_local_ingest_file). REAL but stays LOCAL to the node, never pushed to api.marc27.com.

### End-to-end ingestion matrix: PRISM (main) + marc27-core (fix/audit-tail) — entry points × destinations
- CLI `prism ingest` csv/tsv/parquet -> local Neo4j: WIRED (PRISM/crates/cli/src/main.rs:4643 run_local_ingest_file -> crates/ingest/src/pipeline.rs:180-199 -> crates/ingest/src/graph.rs:270 real Neo4j tx/commit HTTP)
- CLI `prism ingest` csv/tsv/parquet -> local Qdrant: WIRED (pipeline.rs:202-228 -> crates/ingest/src/embeddings.rs:219 upsert; real embeddings via crates/llm/src/lib.rs:389 /v1/embeddings)
- CLI `prism ingest` pdf -> runtime PDF text extraction: WIRED (main.rs:4472 extract_pdf_text_with_runtime -> marc27-core/runtime/main.py:241-249,547 _extract_pymupdf, real PyMuPDF)
- CLI `prism ingest` pdf/txt/md/json/jsonl/owl/cif -> cloud FalkorDB: DEAD-END/BROKEN — see finding 1 (main.rs:4617 submits doc text as source type 'query'; knowledge-service treats query as academic search, doc text never stored)
- CLI `prism ingest --watch`: WIRED to same handlers (main.rs:5033 handle_ingest_watch, real poll loop -> handle_ingest)
- CLI `prism knowledge ingest --url|--query` -> cloud FalkorDB graph + FalkorDB vectors + Postgres provenance: WIRED (main.rs:6387-6400 -> marc27-core/crates/knowledge-service/src/main.rs:1404 submit -> 1623 run_ingest_job: fetch 1726, extract 2017, graph 2047 -> core/src/graph/ingest.rs:119 batch_ingest -> FalkorDB client.rs:23; embeddings 2094-2160 -> core/src/embeddings/search.rs:93 vecf32 store; provenance 2065 Postgres)
- Agent chat tools ingest/ingest_file/ingest_watch/knowledge_ingest -> same as CLI: WIRED (PRISM/crates/agent/src/command_tools.rs:242-265,602-607; executes real prism binary at command_tools.rs:2705-2717; inherits finding 1 for text/PDF formats)
- Agent chat Python knowledge_write embed/embed_bulk/graph_seed/graph_ingest -> cloud FalkorDB graph+vectors: WIRED (PRISM/app/tools/knowledge_write.py:91-173 -> knowledge-service main.rs:703 embed_text, 1174 bulk_embed, 641 graph_ingest, 575 graph_seed — all real, honest errors when graph/vector/embedder unconfigured)
- Agent chat any tool call -> local provenance.db: WIRED (PRISM/crates/agent/src/hooks.rs:302-337 post-hook records inputs+outputs+embedding to ~/.prism/provenance.db; session recall agent_loop.rs:163)
- Python tool results -> local artifacts.db: WIRED (PRISM/app/tools/base.py:74 record_if_enabled -> app/tools/memory/store.py:38-48 SQLite+FTS5 at ~/.prism/artifacts.db)
- PRISM node HTTP POST /api/data/ingest: HONEST-REFUSAL 501 (PRISM/crates/server/src/handlers/data.rs:56-68 — explicit not_implemented, no fake accepted)
- PRISM node HTTP POST /api/tools/{name}/run (ingest command tools): WIRED (server/src/handlers/tools.rs:135-170 -> agent/src/service.rs:274-341 invoke_tool -> execute_command_tool -> prism CLI; durable audit to provenance.db service.rs:345-384)
- Workflows `action: tool` -> node /api/tools/{name}/run: WIRED (PRISM/crates/workflows/src/lib.rs:781-880 run_tool_step, real HTTP + honest 401 without token; no dedicated ingest action exists — dispatch at lib.rs:466-505)
- PRISM native MCP server -> ingest command tools: WIRED (PRISM/crates/cli/src/mcp_server_native.rs:107-127 exposes+executes all command tools)
- marc27-core knowledge-service POST /graph/ingest: WIRED (main.rs:641-668, tenant override from auth, honest error if graph unconfigured -> graph/ingest.rs:119 MERGE cypher)
- marc27-core knowledge-service POST /embed + /embed/bulk: WIRED (main.rs:703-756, 1174-1260 -> embeddings/search.rs:93 FalkorDB vector index, honest unconfigured errors, RBAC corpus write check)
- marc27-core /ingest-job Url source: WIRED (main.rs:1726-1820 SSRF-validated fetch + HTML strip -> real extraction/graph/embed/provenance)
- marc27-core /ingest-job Query source: WIRED to academic search (main.rs:1822-1878 search_all -> title+abstract ingest, honest 'NEVER fall back to ingesting the query string' failure at 1841)
- marc27-core /ingest-job R2Path source, binary PDF: HONEST-REFUSAL (main.rs:1893-1903 'text extraction is not wired on the R2 ingest path')
- marc27-core /ingest-job Corpus source (text files): WIRED (main.rs:1946-2010 R2 list+get -> extraction pipeline)
- marc27-core POST /ingest/source (ntrs/openalex sweep): WIRED (main.rs:1018-1113, honest errors for unconfigured embedder/graph/LLM -> ontology::pipeline::ingest_source)
- marc27-core POST /graph/seed (admin CSV seed): WIRED (main.rs:575-638, admin-gated, real background seed_nodes/seed_edges)
- API gateway /knowledge/* proxy -> knowledge-service: WIRED (marc27-core/crates/api/src/routes/knowledge_proxy.rs:30-60, auth at gateway, admin gate on /graph/seed at 262-271)
- Relay platform->node tool invoke: WIRED (marc27-core/crates/api/src/routes/nodes.rs:143-215 invoke_node_tool -> core/src/compute/protocol/registry.rs:260 WS relay -> PRISM/crates/node/src/daemon.rs:902-988 executes via local ChatService, honest failure when no executor at daemon.rs:917-930; relayed callers cannot self-approve gated tools, service.rs:298-303)
- R2 writes: admin direct+presigned upload WIRED admin-only (marc27-core/crates/api/src/routes/admin_storage.rs:25-26,67-91); research-engine ingest_paper archives paper text to R2 (core/src/research/engine.rs:3341-3353) and writes graph+audit rows (engine.rs:3419-3455); R2Store is a real S3 client (core/src/marketplace/storage.rs:291-359)
- runtime /embed endpoint (Python sidecar): WIRED (marc27-core/runtime/main.py:515 embed_texts, real model)
